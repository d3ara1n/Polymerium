<husk:Dialog
    x:Class="Polymerium.App.Dialogs.PackageBulkUpdatePreviewerDialog"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:app="https://github.com/d3ara1n/Polymerium"
    xmlns:collections="clr-namespace:System.Collections;assembly=System.Runtime"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:fi="clr-namespace:FluentIcons.Avalonia;assembly=FluentIcons.Avalonia"
    xmlns:husk="https://github.com/d3ara1n/Huskui.Avalonia"
    xmlns:lang="https://github.com/d3ara1n/Polymerium/Languages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="{x:Static lang:Resources.PackageBulkUpdatePreviewerDialog_Title}"
    Width="360"
    d:DesignHeight="450"
    d:DesignWidth="800"
    IsPrimaryButtonVisible="True"
    mc:Ignorable="d">
    <StackPanel Spacing="12">
        <DockPanel>
            <ToggleSwitch
                DockPanel.Dock="Right"
                IsChecked="{Binding $parent[app:PackageBulkUpdatePreviewerDialog].IsEnabledOnly, Mode=TwoWay}" />
            <TextBlock
                VerticalAlignment="Center"
                Text="{x:Static lang:Resources.PackageBulkUpdatePreviewerDialog_EnabledOnlyLabelText}" />
        </DockPanel>
        <DockPanel>
            <ComboBox
                DockPanel.Dock="Right"
                DisplayMemberBinding="{Binding x:DataType=app:PackageBulkUpdatePreviewerTagPolicy, Mode=OneTime, Converter={x:Static app:LocalizedEnumConverters.LocalizedPackageBulkUpdatePreviewerTagPolicy}}"
                ItemsSource="{Binding Source={x:Type app:PackageBulkUpdatePreviewerTagPolicy}, Mode=OneTime, x:DataType={x:Type collections:IEnumerable}, Converter={x:Static app:InternalConverters.EnumTypeToList}}"
                SelectedItem="{Binding $parent[app:PackageBulkUpdatePreviewerDialog].TagPolicy, Mode=TwoWay}" />
            <TextBlock
                VerticalAlignment="Center"
                Text="{x:Static lang:Resources.PackageBulkUpdatePreviewerDialog_TagPolicyLabelText}" />
        </DockPanel>
        <ItemsControl ItemsSource="{Binding $parent[app:PackageBulkUpdatePreviewerDialog].Tags}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel
                        ItemSpacing="6"
                        LineSpacing="6" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="x:String">
                    <Border
                        Background="{StaticResource ControlAccentTranslucentFullBackgroundBrush}"
                        BorderBrush="{StaticResource ControlAccentInteractiveBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource SmallCornerRadius}">
                        <DockPanel>
                            <Button
                                DockPanel.Dock="Right"
                                Padding="4,0"
                                Command="{Binding $parent[app:PackageBulkUpdatePreviewerDialog].RemoveTagCommand}"
                                CommandParameter="{Binding}"
                                CornerRadius="{husk:StaticResourceBinding SmallCornerRadius,
                                                                          Converter={x:Static husk:CornerRadiusConverters.Right}}"
                                Theme="{StaticResource GhostButtonTheme}">
                                <fi:SymbolIcon
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center"
                                    FontSize="{Binding $parent[Button].FontSize}"
                                    Symbol="Dismiss" />
                            </Button>
                            <TextBlock
                                Margin="8,4,0,4"
                                Text="{Binding}" />
                        </DockPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Button Command="{Binding $parent[app:PackageBulkUpdatePreviewerDialog].AddTagCommand}">
            <husk:IconLabel
                Icon="Tag"
                Text="{x:Static lang:Resources.PackageBulkUpdatePreviewerDialog_AddTagButtonText}" />
        </Button>
    </StackPanel>
</husk:Dialog>
