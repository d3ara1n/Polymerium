<ResourceDictionary xmlns="https://github.com/avaloniaui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="using:Polymerium.App.Controls"
                    xmlns:husk="https://github.com/d3ara1n/Huskui.Avalonia"
                    xmlns:icons="https://github.com/MahApps/IconPacks.Avalonia"
                    xmlns:m="using:Polymerium.App.Models"
                    xmlns:converters="using:Polymerium.App.Converters">
    <ControlTheme x:Key="{x:Type local:PackageEntryButton}" TargetType="local:PackageEntryButton">
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <ControlTemplate x:DataType="m:InstancePackageModel">
                <husk:Card Name="Background" Height="78" Background="{StaticResource LayerBackgroundBrush}"
                           CornerRadius="{StaticResource MediumCornerRadius}">
                    <husk:Card.Opacity>
                        <MultiBinding Converter="{x:Static converters:InternalConverters.OneOr}">
                            <Binding Path="IsEnabled" Mode="OneWay" />
                            <Binding Source="{StaticResource ControlDimOpacity}" />
                        </MultiBinding>
                    </husk:Card.Opacity>
                    <husk:Card.Transitions>
                        <Transitions>
                            <BrushTransition Property="Background" Easing="SineEaseOut"
                                             Duration="{StaticResource ControlFasterAnimationDuration}" />
                            <BrushTransition Property="BorderBrush" Easing="SineEaseOut"
                                             Duration="{StaticResource ControlFasterAnimationDuration}" />
                            <DoubleTransition Property="Opacity" Easing="SineEaseOut"
                                              Duration="{StaticResource ControlFasterAnimationDuration}" />
                        </Transitions>
                    </husk:Card.Transitions>
                    <Panel>
                        <TextBlock Text="Disabled" FontSize="36" HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   RenderTransform="scaleX(1.2)"
                                   Foreground="{StaticResource ControlSecondaryForegroundBrush}">
                            <TextBlock.Opacity>
                                <MultiBinding Converter="{x:Static converters:InternalConverters.ZeroOr}">
                                    <Binding Path="IsEnabled" Mode="OneWay" />
                                    <Binding Source="{StaticResource Overlay2Opacity}" />
                                </MultiBinding>
                            </TextBlock.Opacity>
                            <TextBlock.Transitions>
                                <Transitions>
                                    <DoubleTransition Property="Opacity" Easing="SineEaseOut"
                                                      Duration="{StaticResource ControlFasterAnimationDuration}" />
                                </Transitions>
                            </TextBlock.Transitions>
                        </TextBlock>
                        <Grid ColumnDefinitions="Auto,8,*" RowDefinitions="Auto,*">
                            <Border Grid.Row="0" Grid.RowSpan="2" Grid.Column="0"
                                    CornerRadius="{StaticResource SmallCornerRadius}" ClipToBounds="True"
                                    Width="{Binding Bounds.Height,RelativeSource={RelativeSource Mode=Self}}">
                                <Border.Background>
                                    <ImageBrush Source="{Binding Thumbnail}" />
                                </Border.Background>
                            </Border>
                            <DockPanel Grid.Row="0" Grid.Column="2">
                                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal"
                                            Spacing="4">
                                    <ContentControl Content="{Binding Version}">
                                        <ContentControl.DataTemplates>
                                            <DataTemplate DataType="m:InstancePackageVersionModel">
                                                <TextBlock Text="{Binding Name}"
                                                           VerticalAlignment="Center"
                                                           FontWeight="{StaticResource ControlStrongFontWeight}" />
                                            </DataTemplate>
                                            <DataTemplate DataType="m:InstancePackageUnspecifiedVersionModel">
                                                <husk:Tag Content="Unspecified" />
                                            </DataTemplate>
                                        </ContentControl.DataTemplates>
                                    </ContentControl>
                                </StackPanel>
                                <Grid ColumnDefinitions="Auto,6,*">
                                    <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="4">
                                        <husk:Tag CornerRadius="{StaticResource SmallCornerRadius}"
                                                  Classes="Primary" IsVisible="{Binding IsLocked}">
                                            <TextBlock Text="Original" />
                                        </husk:Tag>
                                        <husk:Tag CornerRadius="{StaticResource SmallCornerRadius}">
                                            <TextBlock Text="{Binding Kind}" />
                                        </husk:Tag>
                                    </StackPanel>
                                    <TextBlock Grid.Column="2" Text="{Binding ProjectName}"
                                               TextTrimming="CharacterEllipsis"
                                               FontSize="{StaticResource LargeFontSize}"
                                               FontWeight="{StaticResource ControlStrongFontWeight}" />
                                </Grid>
                            </DockPanel>
                            <Grid Grid.Row="1" Grid.Column="2" ColumnDefinitions="*,4,Auto" RowDefinitions="*,0,Auto">
                                <StackPanel Grid.Row="0" Grid.RowSpan="3" Grid.Column="2"
                                            Orientation="Horizontal" VerticalAlignment="Bottom" Spacing="4">
                                    <ItemsControl Name="TagBlock"
                                                  ItemsSource="{Binding Tags}" Opacity="0.0">
                                        <ItemsControl.Transitions>
                                            <Transitions>
                                                <DoubleTransition Property="Opacity" Easing="SineEaseOut"
                                                                  Duration="{StaticResource ControlFasterAnimationDuration}" />
                                            </Transitions>
                                        </ItemsControl.Transitions>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate x:DataType="x:String">
                                                <husk:Tag>
                                                    <TextBlock Text="{Binding}" />
                                                </husk:Tag>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Horizontal" Spacing="4" />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                    </ItemsControl>
                                    <Button Theme="{StaticResource OutlineButtonTheme}" Classes="Small"
                                            Flyout="{TemplateBinding ContextFlyout}">
                                        <icons:PackIconLucide Kind="Ellipsis" Width="12" Height="12" />
                                    </Button>
                                </StackPanel>
                                <Panel Name="SummaryBlock" Grid.Row="0" Grid.Column="0">
                                    <TextBlock Text="{Binding Summary}"
                                               MaxLines="2"
                                               TextWrapping="Wrap" TextTrimming="CharacterEllipsis" />
                                </Panel>
                            </Grid>
                        </Grid>
                    </Panel>
                </husk:Card>
            </ControlTemplate>
        </Setter>

        <Style Selector="^:pointerover /template/ ItemsControl#TagBlock">
            <Setter Property="Opacity" Value="1.0" />
        </Style>

        <Style Selector="^:pointerover /template/ husk|Card#Background">
            <Setter Property="BorderBrush"
                    Value="{StaticResource ControlAccentInteractiveBorderBrush}" />
        </Style>

        <Style Selector="^:pressed /template/ husk|Card#Background">
            <Setter Property="Background"
                    Value="{StaticResource OverlayFullBackgroundBrush}" />
        </Style>

        <Style Selector="^:disabled /template/ husk|Card#Background">
            <Setter Property="Opacity" Value="{StaticResource ControlDimOpacity}" />
        </Style>
    </ControlTheme>
</ResourceDictionary>