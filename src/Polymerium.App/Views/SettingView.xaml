<Page
    x:Class="Polymerium.App.Views.SettingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Polymerium.App.Controls"
    mc:Ignorable="d">
    <ScrollViewer>
        <controls:Card Margin="5,0,10,10" Padding="50">
            <StackPanel Spacing="5">
                <StackPanel.ChildrenTransitions>
                    <EntranceThemeTransition />
                </StackPanel.ChildrenTransitions>
                <TextBlock Text="应用" Style="{StaticResource SubtitleTextBlockStyle}" />
                <controls:ExpanderEx Padding="0">
                    <controls:ExpanderEx.Header>
                        <Grid>
                            <StackPanel Orientation="Horizontal">
                                <FontIcon Glyph="&#xE18A;" Margin="20" />
                                <TextBlock VerticalAlignment="Center">
                                    <Run Text="使用超能力" />
                                </TextBlock>
                            </StackPanel>
                            <ToggleSwitch HorizontalAlignment="Right" Margin="0,0,-90,0" />
                        </Grid>
                    </controls:ExpanderEx.Header>
                    <StackPanel Spacing="1">
                        <Grid Background="{ThemeResource ExpanderHeaderBackground}">
                            <TextBlock Text="其实没有超能力" />
                        </Grid>
                        <Grid Background="{ThemeResource ExpanderHeaderBackground}">
                            <Button Content="点这个按钮也没用" />
                        </Grid>
                    </StackPanel>
                </controls:ExpanderEx>
                <controls:ExpanderEx Padding="0">
                    <controls:ExpanderEx.Header>
                        <Grid>
                            <StackPanel Orientation="Horizontal">
                                <FontIcon Glyph="&#xE164;" Margin="20" />
                                <TextBlock VerticalAlignment="Center">
                                    <Run Text="语言" />
                                    <LineBreak />
                                    <Run Text="重启生效" Foreground="{ThemeResource ApplicationSecondaryForegroundThemeBrush}" />
                                </TextBlock>
                            </StackPanel>
                            <ComboBox SelectedIndex="0" IsEditable="False" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <x:String>简体中文</x:String>
                                <x:String>还是简体中文</x:String>
                            </ComboBox>
                        </Grid>
                    </controls:ExpanderEx.Header>
                </controls:ExpanderEx>
                <TextBlock Text="游戏全局配置" Style="{StaticResource SubtitleTextBlockStyle}" />
                <TextBlock Text="私有实例配置中未设置的项目将会继承全局配置中的对应项目" Foreground="{ThemeResource ApplicationSecondaryForegroundThemeBrush}" />
                <controls:ExpanderEx Padding="0">
                    <controls:ExpanderEx.Header>
                        <Grid>
                            <StackPanel Orientation="Horizontal">
                                <FontIcon Glyph="&#xE8B7;" Margin="20" />
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="Java 可执行文件位置"/>
                                    <TextBlock Text="{x:Bind ViewModel.JavaPath,Mode=TwoWay}" Foreground="{ThemeResource ApplicationSecondaryForegroundThemeBrush}" />
                                </StackPanel>
                            </StackPanel>
                            <Button x:Name="OpenJavaDialogButton" Content="打开选取对话框" HorizontalAlignment="Right"/>
                        </Grid>
                    </controls:ExpanderEx.Header>
                    <Grid Background="{ThemeResource ExpanderHeaderBackground}">
                        <!-- 日后会有对话框用以在多个 Java 中选择 -->
                        <TextBox Text="{x:Bind ViewModel.JavaPath,Mode=TwoWay}"/>
                    </Grid>
                </controls:ExpanderEx>
                <controls:ExpanderEx Padding="0">
                    <controls:ExpanderEx.Header>
                        <Grid>
                            <StackPanel Orientation="Horizontal">
                                <FontIcon Glyph="&#xE8B7;" Margin="20" />
                                <TextBlock VerticalAlignment="Center">
                                <Run Text="Java 虚拟机分配的最大内存" />
                                <LineBreak />
                                <Run Text="没有自动分配算法，根据经验来设置" Foreground="{ThemeResource ApplicationSecondaryForegroundThemeBrush}" />
                                </TextBlock>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right">
                                <TextBox Text="4096" />
                                <TextBlock Text="MB" VerticalAlignment="Center" />
                            </StackPanel>
                        </Grid>
                    </controls:ExpanderEx.Header>
                </controls:ExpanderEx>
                <TextBlock Text="说明" Style="{StaticResource SubtitleTextBlockStyle}" />
                <TextBlock>
                    <Run Text="图标素材来自 " />
                    <Hyperlink NavigateUri="https://icons8.com/icons/fluency">icons8</Hyperlink>
                </TextBlock>
            </StackPanel>
        </controls:Card>
    </ScrollViewer>
</Page>