<?xml version="1.0" encoding="utf-8"?>

<Page
    x:Class="Polymerium.App.Views.WorkbenchView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Polymerium.App.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:toolkit="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:labs="using:CommunityToolkit.WinUI.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5*" />
            <ColumnDefinition Width="1" />
            <ColumnDefinition Width="5*" />
        </Grid.ColumnDefinitions>
        <ScrollViewer Grid.Column="0">
            <StackPanel Margin="{StaticResource PageContentMargin}">
                <Expander HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                    <Expander.Header>
                        <StackPanel Orientation="Horizontal">
                            <CheckBox IsChecked="True" />
                            <TextBlock Text="Custom" VerticalAlignment="Center" />
                            <HyperlinkButton VerticalAlignment="Center">
                                <FontIcon Glyph="&#xE70F;" FontSize="10" />
                            </HyperlinkButton>
                        </StackPanel>
                    </Expander.Header>
                    <StackPanel>
                        <TextBlock Text="Loaders" Style="{StaticResource SubtitleTextBlockStyle}" />
                        <controls:Card Padding="0" HorizontalAlignment="Left" Width="268">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0"
                                        Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                                    <Image Width="64" Source="/Assets/Components/net.neoforged.png"
                                           Margin="{StaticResource MediumMargin}" VerticalAlignment="Center" />
                                </Border>
                                <Grid Grid.Column="1" Margin="{StaticResource CardContentPadding}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <StackPanel Grid.Row="0">
                                        <TextBlock Text="NeoForge" Style="{StaticResource BodyStrongTextBlockStyle}" />
                                        <TextBlock Text="43.11.23" />
                                    </StackPanel>
                                    <TextBlock Grid.Row="1">
                                        <Hyperlink>
                                            <Run Text="Remove" />
                                        </Hyperlink>
                                    </TextBlock>
                                </Grid>
                            </Grid>
                        </controls:Card>
                        <TextBlock Text="Attachments" Style="{StaticResource SubtitleTextBlockStyle}" />
                        <controls:Card HorizontalAlignment="Stretch">
                            <Grid ColumnSpacing="{StaticResource SmallGap}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <toolkit:ImageEx Grid.Column="0"
                                                 Source="https://media.forgecdn.net/avatars/342/357/637486273248094570.png"
                                                 Width="32" />
                                <StackPanel Grid.Column="1">
                                    <StackPanel Orientation="Horizontal" Spacing="{StaticResource TinyGap}">
                                        <Border Background="{ThemeResource SolidBackgroundFillColorBaseBrush}"
                                                Padding="{StaticResource TinyMargin}" HorizontalAlignment="Left">
                                            <TextBlock Text="Mod" Style="{StaticResource CaptionTextBlockStyle}" />
                                        </Border>
                                        <TextBlock Text="Pipez" VerticalAlignment="Center"
                                                   Style="{StaticResource BodyStrongTextBlockStyle}" />
                                    </StackPanel>
                                    <TextBlock
                                        Text="This mod adds simple and highly configurable pipes that are designed to create as little lag as possible."
                                        TextTrimming="WordEllipsis" />
                                </StackPanel>
                                <Grid Grid.Column="2">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Text="Pipez-1.20.1.zip" HorizontalAlignment="Right" />
                                    <StackPanel Grid.Row="1" Orientation="Horizontal"
                                                Spacing="{StaticResource TinyGap}" VerticalAlignment="Bottom"
                                                HorizontalAlignment="Right">
                                        <TextBlock>
                                            <Hyperlink>
                                                <Run Text="Modify" />
                                            </Hyperlink>
                                        </TextBlock>
                                        <TextBlock>
                                            <Hyperlink>
                                                <Run Text="Delete" />
                                            </Hyperlink>
                                        </TextBlock>
                                    </StackPanel>
                                </Grid>
                            </Grid>
                        </controls:Card>
                    </StackPanel>
                </Expander>
            </StackPanel>
        </ScrollViewer>
        <Border Grid.Column="1" BorderBrush="{ThemeResource CardStrokeColorDefaultSolidBrush}"
                BorderThickness="1,0,0,0" />
        <Grid Grid.Column="2">
            <ScrollViewer>
                <StackPanel Margin="{StaticResource PageContentMargin}" Spacing="{StaticResource TinyGap}">
                    <InfoBar IsOpen="True" Message="Turn on the switch to override values from the global."
                             IsClosable="False" />
                    <labs:SettingsExpander Header="Jvm Version"
                                           Description="The directory contains bin/java.exe executable file.">
                        <labs:SettingsExpander.Items>
                            <CheckBox Grid.Column="1" Content="Skip Java runtime version check" />
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Selected Java version in vault"
                                           VerticalAlignment="Center" />
                                <ComboBox Grid.Column="1" Text="8">
                                    <ComboBoxItem Content="8" />
                                    <ComboBoxItem Content="11" />
                                    <ComboBoxItem Content="17" />
                                </ComboBox>
                            </Grid>
                        </labs:SettingsExpander.Items>
                        <ToggleSwitch />
                    </labs:SettingsExpander>
                    <labs:SettingsExpander Header="Jvm Max Memory"
                                           Description="The max heap memory that jvm can allocate.">
                        <labs:SettingsExpander.Items>
                            <TextBox Text="8192" />
                        </labs:SettingsExpander.Items>
                        <ToggleSwitch />
                    </labs:SettingsExpander>
                    <labs:SettingsExpander Header="Jvm Addition Arguments"
                                           Description="These arguments will be appended after jvm created.">
                        <labs:SettingsExpander.Items>
                            <TextBox
                                Text="-XX:HeapDumpPath=MojangTricksIntelDriversForPerformance_javaw.exe_minecraft.exe.heapdump -Xms256m" />
                        </labs:SettingsExpander.Items>
                        <ToggleSwitch />
                    </labs:SettingsExpander>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>