<?xml version="1.0" encoding="utf-8"?>

<Page
    x:Class="Polymerium.App.Views.MetadataView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Polymerium.App.Controls"
    xmlns:models="using:Polymerium.App.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Margin="{StaticResource PageContentMargin}" ColumnSpacing="{StaticResource SmallGap}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="7*" />
        </Grid.ColumnDefinitions>
        <controls:Card Grid.Column="0" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Margin="{StaticResource CardContentUpperMargin}">
                    <TextBlock Text="Version" Foreground="{ThemeResource ApplicationSecondaryForegroundThemeBrush}" />
                    <TextBlock Text="1.20.1" Style="{StaticResource TitleLargeTextBlockStyle}" />
                </StackPanel>
                <ScrollViewer Grid.Row="1">
                    <ListView x:Name="LayerList" ItemsSource="{x:Bind ViewModel.Model.Layers,Mode=OneWay}">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="models:MetadataModel">
                                <StackPanel>
                                    <TextBlock Text="Summary" />
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </ScrollViewer>
            </Grid>
        </controls:Card>
        <Grid Grid.Column="1" RowSpacing="{StaticResource SmallGap}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <controls:Card Grid.Row="0" Height="128">
                <ListView>
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:LoaderModel">
                            <controls:Card Padding="0" HorizontalAlignment="Left" Width="268"
                                           CornerRadius="{StaticResource CardMediumCornerRadius}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0"
                                            Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                                        <Image Width="64" Source="{x:Bind Thumbnail}"
                                               Margin="{StaticResource MediumMargin}" VerticalAlignment="Center" />
                                    </Border>
                                    <Grid Grid.Column="1" Margin="{StaticResource CardContentMargin}">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <StackPanel Grid.Row="0">
                                            <TextBlock Text="{x:Bind Name}"
                                                       Style="{StaticResource BodyStrongTextBlockStyle}" />
                                            <TextBlock Text="{x:Bind Inner.Version}" />
                                        </StackPanel>
                                        <TextBlock Grid.Row="1">
                                            <Hyperlink>
                                                <Run Text="Remove" />
                                            </Hyperlink>
                                        </TextBlock>
                                    </Grid>
                                </Grid>
                            </controls:Card>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </controls:Card>
            <controls:Card Grid.Row="1" />
        </Grid>
    </Grid>
</Page>