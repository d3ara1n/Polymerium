<!-- Copyright (c) Microsoft Corporation and Contributors. -->
<!-- Licensed under the MIT License. -->

<Page
    x:Class="Polymerium.App.Views.InstanceView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:controls="using:Polymerium.App.Controls"
    xmlns:models="using:Polymerium.App.Models"
    mc:Ignorable="d">
    <ScrollViewer>
        <StackPanel>
            <Grid x:Name="Header" Background="{ThemeResource PageBackgroundBrush}" BorderBrush="{ThemeResource PageStrokeBrush}"
              BorderThickness="1" CornerRadius="5" Height="220" Margin="5,0,10,10" ColumnDefinitions="Auto,*" PointerEntered="Header_PointerEntered" PointerExited="Header_PointerExited">
                <Grid.ChildrenTransitions>
                    <ContentThemeTransition/>
                </Grid.ChildrenTransitions>
                <toolkit:ConstrainedBox Margin="30,30,15,30" VerticalAlignment="Center" HorizontalAlignment="Left" AspectRatio="1:1">
                    <Border CornerRadius="5" BorderThickness="1" BorderBrush="{ThemeResource PageStrokeBrush}">
                        <toolkit:ImageEx CornerRadius="5" Source="ms-appx:///Assets/Placeholders/default_world_icon.png"/>
                    </Border>
                </toolkit:ConstrainedBox>
                <Grid Grid.Column="1" Margin="15,30,30,30" RowDefinitions="Auto,*">
                    <Grid ColumnDefinitions="*,Auto">
                        <StackPanel>
                            <TextBlock Text="All The Mods 8" Style="{StaticResource TitleTextBlockStyle}"/>
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <TextBlock Foreground="{ThemeResource ApplicationSecondaryForegroundThemeBrush}">
                                <Run>来自</Run>
                                <Hyperlink>Curseforge</Hyperlink>
                                </TextBlock>
                            </StackPanel>
                            <TextBlock Text="游戏版本 1.18.2" Foreground="{ThemeResource ApplicationSecondaryForegroundThemeBrush}"/>
                        </StackPanel>
                        <Grid Grid.Column="1">
                            <Button VerticalAlignment="Top" HorizontalAlignment="Right" Content="运行" Width="120" Style="{StaticResource AccentButtonStyle}"/>
                        </Grid>
                    </Grid>
                    <Grid Grid.Row="1" ColumnDefinitions="*,Auto">
                        <ScrollViewer  VerticalScrollBarVisibility="Auto" VerticalScrollMode="Enabled" HorizontalScrollMode="Disabled" HorizontalScrollBarVisibility="Disabled">
                            <GridView SelectionMode="None" Padding="0" VerticalAlignment="Bottom">
                                <GridView.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <toolkit:WrapPanel/>
                                    </ItemsPanelTemplate>
                                </GridView.ItemsPanel>
                                <GridView.ItemContainerStyle>
                                    <Style TargetType="GridViewItem">
                                        <Setter Property="Margin" Value="0"/>
                                    </Style>
                                </GridView.ItemContainerStyle>
                                <GridView.ItemTemplate>
                                    <DataTemplate x:DataType="models:ExtenderTagItemModel">
                                        <Grid Margin="5,0,5,0">
                                            <Border CornerRadius="5" Background="{ThemeResource FloatLayerBackgroundBrush}" ToolTipService.ToolTip="{x:Bind Version}">
                                                <Grid Margin="10,5,10,5">
                                                    <TextBlock Text="{x:Bind Name}"/>
                                                </Grid>
                                            </Border>
                                        </Grid>
                                    </DataTemplate>
                                </GridView.ItemTemplate>
                                <models:ExtenderTagItemModel Name="Forge" Version="40.2.0"/>
                                <models:ExtenderTagItemModel Name="Optfine" Version="HD_U_H7"/>
                            </GridView>
                        </ScrollViewer>
                        <Grid Grid.Column="1">
                            <controls:IconButton x:Name="EditButton" Opacity="0" HorizontalAlignment="Right" VerticalAlignment="Bottom" ToolTipService.ToolTip="修改实例的版本，体验扩充资源">
                                <controls:IconButton.OpacityTransition>
                                    <ScalarTransition/>
                                </controls:IconButton.OpacityTransition>
                                <FontIcon Glyph="&#xE104;"/>
                            </controls:IconButton>
                        </Grid>
                    </Grid>
                </Grid>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</Page>
