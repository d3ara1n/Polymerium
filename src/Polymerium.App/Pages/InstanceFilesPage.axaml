<app:Subpage
    x:Class="Polymerium.App.Pages.InstanceFilesPage"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:app="https://github.com/d3ara1n/Polymerium"
    xmlns:async="clr-namespace:AsyncImageLoader;assembly=AsyncImageLoader.Avalonia"
    xmlns:collections="clr-namespace:System.Collections;assembly=System.Runtime"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:fi="clr-namespace:FluentIcons.Avalonia;assembly=FluentIcons.Avalonia"
    xmlns:fie="clr-namespace:FluentIcons.Avalonia.Markup;assembly=FluentIcons.Avalonia"
    xmlns:husk="https://github.com/d3ara1n/Huskui.Avalonia"
    xmlns:lang="https://github.com/d3ara1n/Polymerium/Languages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    x:DataType="app:InstanceFilesPageModel"
    ScrollViewer.VerticalScrollBarVisibility="Disabled"
    mc:Ignorable="d">
    <TabControl Padding="0">
        <TabItem>
            <TabItem.Header>
                <StackPanel
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock Text="{x:Static lang:Resources.AssetKind_Mod}" />
                    <husk:PlaceholderContainer Source="{Binding Mods}">
                        <husk:PlaceholderContainer.SourceTemplate>
                            <DataTemplate DataType="collections:IList">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="(" />
                                    <TextBlock Text="{Binding Count, FallbackValue=(0)}" />
                                    <TextBlock
                                        IsVisible="{Binding $parent[app:InstanceFilesPage].((app:InstanceFilesPageModel)DataContext).PackageCount, Converter={x:Static husk:NumberConverters.IsNonZero}, FallbackValue=False}"
                                        Text="+" />
                                    <TextBlock Text=")" />
                                </StackPanel>
                            </DataTemplate>
                        </husk:PlaceholderContainer.SourceTemplate>
                        <husk:ProgressRing
                            Width="{Binding $parent[TabItem].FontSize}"
                            Height="{Binding $parent[TabItem].FontSize}"
                            IsIndeterminate="{Binding Mods, Converter={x:Static ObjectConverters.IsNull}}" />
                    </husk:PlaceholderContainer>
                </StackPanel>
            </TabItem.Header>
            <Grid
                RowDefinitions="Auto,*"
                RowSpacing="12">
                <husk:InfoBar
                    Grid.Row="0"
                    IsVisible="{Binding PackageCount, Converter={x:Static husk:NumberConverters.IsNonZero}}">
                    <Grid
                        ColumnDefinitions="Auto,*,Auto"
                        ColumnSpacing="12">
                        <Border
                            Grid.Column="0"
                            VerticalAlignment="Center"
                            CornerRadius="{StaticResource SmallCornerRadius}">
                            <fi:SymbolIcon
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontSize="{StaticResource ExtraLargeFontSize}"
                                Symbol="Info" />
                        </Border>
                        <StackPanel
                            Grid.Column="1"
                            VerticalAlignment="Center">
                            <TextBlock Text="{x:Static lang:Resources.InstanceFilesPage_ModInfoBarTitle}" />
                            <TextBlock
                                Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                Text="{x:Static lang:Resources.InstanceFilesPage_ModInfoBarText}" />
                        </StackPanel>
                        <Button
                            Grid.Column="2"
                            Classes="Primary"
                            Padding="12,6"
                            VerticalAlignment="Center"
                            Command="{Binding $parent[app:InstancePage].NavigateCommand}"
                            CommandParameter="{x:Type app:InstanceSetupPage}">
                            <husk:IconLabel
                                FontSize="{StaticResource MediumFontSize}"
                                FontWeight="{StaticResource ControlStrongFontWeight}"
                                Icon="Box"
                                Text="{Binding PackageCount}" />
                        </Button>
                    </Grid>
                </husk:InfoBar>
                <Grid
                    Grid.Row="1"
                    ColumnDefinitions="*,4,*"
                    ColumnSpacing="12">
                    <!--  左侧：Mod 列表  -->
                    <Grid
                        Grid.Column="0"
                        RowDefinitions="Auto,*"
                        RowSpacing="8">
                        <!--  搜索和过滤栏  -->
                        <TextBox
                            Grid.Row="0"
                            PlaceholderText="Search mods..."
                            Text="{Binding ModSearchText, Mode=TwoWay}">
                            <TextBox.InnerRightContent>
                                <StackPanel Orientation="Horizontal">
                                    <Button
                                        IsVisible="{Binding $parent[TextBox].Text, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
                                        Command="{Binding $parent[TextBox].Clear}"
                                        Content="{fie:SymbolIcon Symbol=Dismiss,
                                                                 FontSize={StaticResource MediumFontSize}}"
                                        Foreground="{StaticResource ControlSecondaryForegroundBrush}" />

                                    <!--  过滤器面板  -->
                                    <Button Classes.Primary="{Binding IsModFilterActive, Mode=OneWay}">
                                        <Button.Flyout>
                                            <Flyout>
                                                <StackPanel
                                                    MinWidth="320"
                                                    MaxWidth="420"
                                                    Margin="12"
                                                    Spacing="8">
                                                    <TextBlock
                                                        VerticalAlignment="Center"
                                                        FontSize="{StaticResource SmallFontSize}"
                                                        Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                        Text="{x:Static lang:Resources.InstanceFilesPage_FilterLabelText}" />

                                                    <!--  启用与禁用  -->
                                                    <Grid
                                                        ColumnDefinitions="*,Auto"
                                                        ColumnSpacing="24">
                                                        <TextBlock
                                                            Grid.Column="0"
                                                            VerticalAlignment="Bottom"
                                                            Text="{x:Static lang:Resources.InstanceFilesPage_StatusFilterText}" />
                                                        <TabStrip
                                                            Grid.Column="1"
                                                            SelectedItem="{Binding ModFilterEnability, Mode=TwoWay}"
                                                            Theme="{StaticResource SegmentedTabStripTheme}">
                                                            <TabStrip.ItemTemplate>
                                                                <DataTemplate DataType="app:FilterModel">
                                                                    <TextBlock Text="{Binding Label}" />
                                                                </DataTemplate>
                                                            </TabStrip.ItemTemplate>
                                                            <app:FilterModel
                                                                Label="{x:Static lang:Resources.Enum_All}"
                                                                Value="{x:Null}" />
                                                            <app:FilterModel
                                                                Label="{x:Static lang:Resources.Enum_Enabled}"
                                                                Value="{x:True}" />
                                                            <app:FilterModel
                                                                Label="{x:Static lang:Resources.Enum_Disabled}"
                                                                Value="{x:False}" />
                                                        </TabStrip>
                                                    </Grid>

                                                    <husk:Divider />

                                                    <!--  是否上锁  -->
                                                    <Grid
                                                        ColumnDefinitions="*,Auto"
                                                        ColumnSpacing="24">
                                                        <TextBlock
                                                            Grid.Column="0"
                                                            VerticalAlignment="Bottom"
                                                            Text="{x:Static lang:Resources.InstanceFilesPage_SourceFilterText}" />
                                                        <TabStrip
                                                            Grid.Column="1"
                                                            SelectedItem="{Binding ModFilterLockility, Mode=TwoWay}"
                                                            Theme="{StaticResource SegmentedTabStripTheme}">
                                                            <TabStrip.ItemTemplate>
                                                                <DataTemplate DataType="app:FilterModel">
                                                                    <TextBlock Text="{Binding Label}" />
                                                                </DataTemplate>
                                                            </TabStrip.ItemTemplate>
                                                            <app:FilterModel
                                                                Label="{x:Static lang:Resources.Enum_All}"
                                                                Value="{x:Null}" />
                                                            <app:FilterModel
                                                                Label="{x:Static lang:Resources.InstanceFilesPage_SourceImportText}"
                                                                Value="{x:True}" />
                                                            <app:FilterModel
                                                                Label="{x:Static lang:Resources.InstanceFilesPage_SourceLocalText}"
                                                                Value="{x:False}" />
                                                        </TabStrip>
                                                    </Grid>

                                                    <husk:Divider />

                                                    <!--  加载器类型  -->
                                                    <Grid
                                                        ColumnDefinitions="*,Auto"
                                                        ColumnSpacing="24">
                                                        <TextBlock
                                                            Grid.Column="0"
                                                            VerticalAlignment="Bottom"
                                                            Text="{x:Static lang:Resources.InstanceFilesPage_LoaderFilterText}" />
                                                        <TabStrip
                                                            Grid.Column="1"
                                                            SelectedItem="{Binding ModFilterLoader, Mode=TwoWay}"
                                                            Theme="{StaticResource SegmentedTabStripTheme}">
                                                            <TabStrip.ItemTemplate>
                                                                <DataTemplate DataType="app:FilterModel">
                                                                    <TextBlock Text="{Binding Label}" />
                                                                </DataTemplate>
                                                            </TabStrip.ItemTemplate>
                                                            <app:FilterModel
                                                                Label="{x:Static lang:Resources.Enum_All}"
                                                                Value="{x:Null}" />
                                                            <app:FilterModel
                                                                Label="Fabric"
                                                                Value="{x:Static app:ModLoaderKind.Fabric}" />
                                                            <app:FilterModel
                                                                Label="Forge"
                                                                Value="{x:Static app:ModLoaderKind.Forge}" />
                                                            <app:FilterModel
                                                                Label="Quilt"
                                                                Value="{x:Static app:ModLoaderKind.Quilt}" />
                                                            <app:FilterModel
                                                                Label="NeoForge"
                                                                Value="{x:Static app:ModLoaderKind.NeoForge}" />
                                                        </TabStrip>
                                                    </Grid>
                                                </StackPanel>
                                            </Flyout>
                                        </Button.Flyout>
                                        <fi:SymbolIcon
                                            Height="{StaticResource MediumFontSize}"
                                            FontSize="{StaticResource MediumFontSize}"
                                            Symbol="Filter" />
                                    </Button>
                                </StackPanel>
                            </TextBox.InnerRightContent>
                        </TextBox>

                        <!--  Mod 列表  -->
                        <ScrollViewer Grid.Row="1">

                            <app:EmptyContainer IsEmpty="{Binding Mods.Count, Converter={x:Static husk:NumberConverters.IsZero}, FallbackValue=True}">

                                <!--  Mod 列表  -->
                                <ListBox
                                    ItemsSource="{Binding Mods}"
                                    SelectedItem="{Binding SelectedMod, Mode=TwoWay}"
                                    SelectionMode="Single"
                                    Theme="{StaticResource CleanListBoxTheme}">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate DataType="app:AssetModModel">
                                            <Grid
                                                ColumnDefinitions="Auto,*,Auto"
                                                ColumnSpacing="12">
                                                <!--  Mod 图标  -->
                                                <Border
                                                    Grid.Column="0"
                                                    Width="48"
                                                    Height="48"
                                                    CornerRadius="{StaticResource SmallCornerRadius}">
                                                    <Border.Background>
                                                        <ImageBrush
                                                            Source="{Binding Icon}"
                                                            Stretch="Uniform" />
                                                    </Border.Background>
                                                </Border>

                                                <!--  Mod 信息  -->
                                                <StackPanel
                                                    Grid.Column="1"
                                                    VerticalAlignment="Center">
                                                    <DockPanel>

                                                        <!--  状态指示器  -->
                                                        <husk:SwitchContainer
                                                            DockPanel.Dock="Right"
                                                            Width="8"
                                                            Height="8"
                                                            VerticalAlignment="Top"
                                                            TargetType="x:Boolean"
                                                            Value="{Binding IsLocked}">
                                                            <husk:SwitchCase Value="{x:False}">
                                                                <husk:SwitchContainer
                                                                    Width="8"
                                                                    Height="8"
                                                                    TargetType="x:Boolean"
                                                                    Value="{Binding IsEnabled}">
                                                                    <husk:SwitchCase Value="{x:True}">
                                                                        <Border
                                                                            Background="{StaticResource ControlSuccessBackgroundBrush}"
                                                                            CornerRadius="4" />
                                                                    </husk:SwitchCase>
                                                                    <husk:SwitchCase Value="{x:False}">
                                                                        <Border
                                                                            Background="{StaticResource ControlDangerBackgroundBrush}"
                                                                            CornerRadius="4" />
                                                                    </husk:SwitchCase>
                                                                </husk:SwitchContainer>
                                                            </husk:SwitchCase>
                                                            <husk:SwitchCase Value="{x:True}">
                                                                <Border
                                                                    Background="{StaticResource ControlWarningBackgroundBrush}"
                                                                    CornerRadius="4" />
                                                            </husk:SwitchCase>
                                                        </husk:SwitchContainer>

                                                        <!--  模组名  -->
                                                        <TextBlock
                                                            FontWeight="{StaticResource ControlStrongFontWeight}"
                                                            Text="{Binding DisplayName}"
                                                            TextTrimming="CharacterEllipsis" />
                                                    </DockPanel>

                                                    <!--  描述  -->
                                                    <TextBlock
                                                        FontSize="{StaticResource SmallFontSize}"
                                                        Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                        MaxLines="1"
                                                        Text="{Binding Description}"
                                                        TextTrimming="CharacterEllipsis" />
                                                    <DockPanel>

                                                        <!--  版本  -->
                                                        <husk:Tag
                                                            DockPanel.Dock="Right"
                                                            Classes="Small"
                                                            Content="{Binding Version}" />

                                                        <!--  文件大小  -->
                                                        <TextBlock
                                                            FontSize="{StaticResource SmallFontSize}"
                                                            Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                            Text="{Binding FileSize}" />
                                                    </DockPanel>
                                                </StackPanel>


                                            </Grid>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </app:EmptyContainer>
                        </ScrollViewer>
                    </Grid>

                    <!--  分隔线  -->
                    <GridSplitter
                        Grid.Column="1"
                        Width="2"
                        ResizeDirection="Columns" />

                    <!--  右侧：详细信息面板  -->
                    <ScrollViewer Grid.Column="2">
                        <husk:PlaceholderContainer Source="{Binding SelectedMod}">
                            <husk:PlaceholderContainer.SourceTemplate>
                                <DataTemplate DataType="app:AssetModModel">
                                    <StackPanel Spacing="8">
                                        <!--  Mod 头部  -->
                                        <Grid
                                            ColumnDefinitions="Auto,*"
                                            ColumnSpacing="8">
                                            <Border
                                                Grid.Column="0"
                                                Width="82"
                                                Height="82"
                                                CornerRadius="{StaticResource MediumCornerRadius}">
                                                <Border.Background>
                                                    <ImageBrush
                                                        Source="{Binding Icon}"
                                                        Stretch="Uniform" />
                                                </Border.Background>
                                            </Border>
                                            <StackPanel
                                                Grid.Column="1"
                                                VerticalAlignment="Center"
                                                Spacing="4">
                                                <TextBlock
                                                    FontSize="{StaticResource ExtraLargeFontSize}"
                                                    FontWeight="{StaticResource ControlStrongFontWeight}"
                                                    Text="{Binding DisplayName}"
                                                    TextTrimming="CharacterEllipsis" />
                                                <TextBlock
                                                    Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                    Text="{Binding Author}"
                                                    TextTrimming="CharacterEllipsis" />
                                                <StackPanel
                                                    Orientation="Horizontal"
                                                    Spacing="4">
                                                    <husk:Tag
                                                        Classes="Primary"
                                                        IsVisible="{Binding IsLocked}">
                                                        <husk:IconLabel
                                                            Icon="LockClosed"
                                                            Text="{x:Static lang:Resources.InstancePackageButton_ImportTagText}" />
                                                    </husk:Tag>
                                                    <husk:Tag
                                                        IsVisible="{Binding Metadata.LoaderType, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
                                                        Content="{Binding Metadata.LoaderType}" />
                                                    <husk:Tag Content="{Binding Version}" />
                                                </StackPanel>
                                            </StackPanel>
                                        </Grid>

                                        <!--  操作按钮  -->
                                        <husk:ButtonGroup>
                                            <Button
                                                Command="{Binding $parent[app:InstanceFilesPage].((app:InstanceFilesPageModel)DataContext).ToggleModCommand, FallbackValue={x:Null}}"
                                                CommandParameter="{Binding}">
                                                <husk:SwitchContainer
                                                    TargetType="x:Boolean"
                                                    Value="{Binding IsEnabled}">
                                                    <husk:SwitchCase Value="{x:True}">
                                                        <husk:IconLabel
                                                            Icon="Power"
                                                            Text="{x:Static lang:Resources.Enum_Disabled}" />
                                                    </husk:SwitchCase>
                                                    <husk:SwitchCase Value="{x:False}">
                                                        <husk:IconLabel
                                                            Icon="Power"
                                                            Text="{x:Static lang:Resources.Enum_Enabled}" />
                                                    </husk:SwitchCase>
                                                </husk:SwitchContainer>
                                            </Button>
                                            <Button
                                                Command="{Binding $parent[app:InstanceFilesPage].((app:InstanceFilesPageModel)DataContext).OpenFolderCommand, FallbackValue={x:Null}}"
                                                CommandParameter="{Binding FilePath}">
                                                <fi:SymbolIcon
                                                    FontSize="{StaticResource MediumFontSize}"
                                                    Symbol="FolderOpen" />
                                            </Button>
                                            <Button
                                                Command="{Binding $parent[app:InstanceFilesPage].((app:InstanceFilesPageModel)DataContext).DeleteModCommand, FallbackValue={x:Null}}"
                                                CommandParameter="{Binding}">
                                                <fi:SymbolIcon
                                                    FontSize="{StaticResource MediumFontSize}"
                                                    Foreground="{StaticResource ControlDangerForegroundBrush}"
                                                    Symbol="Delete" />
                                            </Button>
                                        </husk:ButtonGroup>

                                        <!--  Mod 描述  -->
                                        <husk:Card
                                            IsVisible="{Binding Metadata.Description, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
                                            BorderThickness="0">
                                            <StackPanel Spacing="8">
                                                <TextBlock
                                                    FontWeight="{StaticResource ControlStrongFontWeight}"
                                                    Text="{x:Static lang:Resources.InstanceFilesPage_DescriptionLabelText}" />
                                                <TextBlock
                                                    Text="{Binding Metadata.Description, FallbackValue=No description available}"
                                                    TextWrapping="Wrap" />
                                            </StackPanel>
                                        </husk:Card>

                                        <!--  详细信息  -->
                                        <husk:Card BorderThickness="0">
                                            <StackPanel Spacing="12">
                                                <TextBlock
                                                    FontWeight="{StaticResource ControlStrongFontWeight}"
                                                    Text="{x:Static lang:Resources.InstanceFilesPage_DetailsLabelText}" />
                                                <Grid
                                                    ColumnDefinitions="Auto,*"
                                                    ColumnSpacing="16"
                                                    RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto"
                                                    RowSpacing="8">
                                                    <TextBlock
                                                        Grid.Row="0"
                                                        Grid.Column="0"
                                                        Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                        Text="{x:Static lang:Resources.InstanceFilesPage_AssetFileNameLabelText}" />
                                                    <TextBlock
                                                        Grid.Row="0"
                                                        Grid.Column="1"
                                                        Text="{Binding FileName, Mode=OneWay}"
                                                        TextWrapping="Wrap" />

                                                    <TextBlock
                                                        Grid.Row="1"
                                                        Grid.Column="0"
                                                        IsVisible="{Binding Metadata.ModId, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
                                                        Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                        Text="{x:Static lang:Resources.InstanceFilesPage_AssetModIdLabelText}" />
                                                    <TextBlock
                                                        Grid.Row="1"
                                                        Grid.Column="1"
                                                        IsVisible="{Binding Metadata.ModId, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
                                                        Text="{Binding Metadata.ModId, FallbackValue=Unknown}" />

                                                    <TextBlock
                                                        Grid.Row="2"
                                                        Grid.Column="0"
                                                        IsVisible="{Binding Metadata.License, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
                                                        Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                        Text="{x:Static lang:Resources.InstanceFilesPage_AssetLicenseLabelText}" />
                                                    <TextBlock
                                                        Grid.Row="2"
                                                        Grid.Column="1"
                                                        IsVisible="{Binding Metadata.License, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
                                                        Text="{Binding Metadata.License, FallbackValue=Unknown}" />

                                                    <TextBlock
                                                        Grid.Row="3"
                                                        Grid.Column="0"
                                                        Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                        Text="{x:Static lang:Resources.InstanceFilesPage_AssetFileSizeLabelText}" />
                                                    <TextBlock
                                                        Grid.Row="3"
                                                        Grid.Column="1"
                                                        Text="{Binding FileSize}" />

                                                    <TextBlock
                                                        Grid.Row="4"
                                                        Grid.Column="0"
                                                        Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                        Text="{x:Static lang:Resources.InstanceFilesPage_AssetLastModifiedLabelText}" />
                                                    <TextBlock
                                                        Grid.Row="4"
                                                        Grid.Column="1"
                                                        Text="{Binding LastModifiedFormatted}" />

                                                    <TextBlock
                                                        Grid.Row="5"
                                                        Grid.Column="0"
                                                        Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                        Text="{x:Static lang:Resources.InstanceFilesPage_AssetStatusLabelText}" />
                                                    <husk:SwitchContainer
                                                        Grid.Row="5"
                                                        Grid.Column="1"
                                                        TargetType="x:Boolean"
                                                        Value="{Binding IsEnabled}">
                                                        <husk:SwitchCase Value="{x:True}">
                                                            <TextBlock
                                                                Foreground="{StaticResource ControlSuccessForegroundBrush}"
                                                                Text="{x:Static lang:Resources.Enum_Enabled}" />
                                                        </husk:SwitchCase>
                                                        <husk:SwitchCase Value="{x:False}">
                                                            <TextBlock
                                                                Foreground="{StaticResource ControlDangerForegroundBrush}"
                                                                Text="{x:Static lang:Resources.Enum_Disabled}" />
                                                        </husk:SwitchCase>
                                                    </husk:SwitchContainer>
                                                </Grid>
                                            </StackPanel>
                                        </husk:Card>

                                        <!--  链接  -->
                                        <husk:Card
                                            IsVisible="{Binding Metadata.Homepage, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
                                            BorderThickness="0">
                                            <Button
                                                Command="{x:Static app:InternalCommands.OpenUriCommand}"
                                                CommandParameter="{Binding Metadata.Homepage}">
                                                <StackPanel
                                                    Orientation="Horizontal"
                                                    Spacing="8">
                                                    <husk:IconLabel
                                                        Icon="Open"
                                                        Text="{x:Static lang:Resources.InstanceFilesPage_AssetVisitWebsiteButtonText}" />
                                                </StackPanel>
                                            </Button>
                                        </husk:Card>
                                    </StackPanel>
                                </DataTemplate>
                            </husk:PlaceholderContainer.SourceTemplate>

                            <!--  空状态  -->
                            <StackPanel
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Spacing="12">
                                <fi:SymbolIcon
                                    HorizontalAlignment="Center"
                                    FontSize="32"
                                    Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                    Symbol="Hexagon" />
                                <TextBlock
                                    FontSize="{StaticResource LargeFontSize}"
                                    Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                    Text="{x:Static lang:Resources.InstanceFilesPage_AssetEmptyPrompt}" />
                            </StackPanel>
                        </husk:PlaceholderContainer>
                    </ScrollViewer>
                </Grid>
            </Grid>
        </TabItem>
        <TabItem>
            <TabItem.Header>
                <StackPanel
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock Text="{x:Static lang:Resources.AssetKind_Server}" />
                    <husk:PlaceholderContainer Source="{Binding Servers}">
                        <husk:PlaceholderContainer.SourceTemplate>
                            <DataTemplate DataType="collections:IList">
                                <TextBlock Text="{Binding Count, StringFormat={}({0}), FallbackValue=(0)}" />
                            </DataTemplate>
                        </husk:PlaceholderContainer.SourceTemplate>
                        <husk:ProgressRing
                            Width="{Binding $parent[TabItem].FontSize}"
                            Height="{Binding $parent[TabItem].FontSize}"
                            IsIndeterminate="{Binding Servers, Converter={x:Static ObjectConverters.IsNull}}" />
                    </husk:PlaceholderContainer>
                </StackPanel>
            </TabItem.Header>
            <Grid
                RowDefinitions="Auto,*"
                RowSpacing="12">
                <DockPanel
                    Grid.Row="0"
                    IsVisible="False">
                    <DockPanel.Styles>
                        <Style Selector="Button">
                            <Setter Property="Classes.Small" Value="True" />
                        </Style>
                    </DockPanel.Styles>
                    <StackPanel>
                        <Button Command="{Binding LoadServersStatusCommand}">
                            <husk:IconLabel
                                Icon="ArrowClockwise"
                                Text="刷新全部" />
                        </Button>
                    </StackPanel>
                    <StackPanel
                        DockPanel.Dock="Right"
                        HorizontalAlignment="Right"
                        Orientation="Horizontal"
                        Spacing="8">
                        <Button Classes="Danger">
                            <husk:IconLabel
                                Icon="Delete"
                                Text="删除" />
                        </Button>
                        <Button Classes="Primary">
                            <husk:IconLabel
                                Icon="Add"
                                Text="添加服务器" />
                        </Button>
                    </StackPanel>
                </DockPanel>

                <ScrollViewer Grid.Row="1">
                    <app:EmptyContainer IsEmpty="{Binding Servers.Count, Converter={x:Static husk:NumberConverters.IsZero}, FallbackValue=True}">
                        <ListBox
                            ItemsSource="{Binding Servers}"
                            SelectedItem="{Binding SelectedServer, Mode=TwoWay}"
                            SelectionMode="Single"
                            Theme="{StaticResource CleanListBoxTheme}">
                            <ListBox.ItemTemplate>
                                <DataTemplate DataType="app:AssetServerModel">
                                    <DockPanel>
                                        <StackPanel
                                            DockPanel.Dock="Bottom"
                                            Margin="0,8,0,0"
                                            IsVisible="{Binding HasLiveStatus}"
                                            Spacing="8">
                                            <StackPanel
                                                Orientation="Horizontal"
                                                Spacing="6">
                                                <husk:Tag>
                                                    <TextBlock>
                                                        <Run Text="{Binding OnlinePlayers}" />
                                                        <Run Text="/" />
                                                        <Run Text="{Binding MaxPlayers}" />
                                                    </TextBlock>
                                                </husk:Tag>
                                                <husk:Tag Content="{Binding VersionName}" />
                                                <husk:SwitchContainer
                                                    TargetType="x:Boolean"
                                                    Value="{Binding Metadata.AcceptTextures}">
                                                    <husk:SwitchCase Value="{x:True}">
                                                        <husk:Tag>
                                                            <TextBlock>
                                                                <Run Text="{x:Static lang:Resources.InstanceFilesPage_ServerResourcePackPreferenceLabelText}" />
                                                                <Run Text=": " />
                                                                <Run Text="{x:Static lang:Resources.InstanceFilesPage_ServerResourcePackAcceptText}" />
                                                            </TextBlock>
                                                        </husk:Tag>
                                                    </husk:SwitchCase>
                                                    <husk:SwitchCase Value="{x:False}">
                                                        <husk:Tag>
                                                            <TextBlock>
                                                                <Run Text="{x:Static lang:Resources.InstanceFilesPage_ServerResourcePackPreferenceLabelText}" />
                                                                <Run Text=": " />
                                                                <Run Text="{x:Static lang:Resources.InstanceFilesPage_ServerResourcePackRejectText}" />
                                                            </TextBlock>
                                                        </husk:Tag>
                                                    </husk:SwitchCase>
                                                    <husk:SwitchCase Value="{x:Null}">
                                                        <husk:Tag>
                                                            <TextBlock>
                                                                <Run Text="{x:Static lang:Resources.InstanceFilesPage_ServerResourcePackPreferenceLabelText}" />
                                                                <Run Text=": " />
                                                                <Run Text="{x:Static lang:Resources.InstanceFilesPage_ServerResourcePackPromptText}" />
                                                            </TextBlock>
                                                        </husk:Tag>
                                                    </husk:SwitchCase>
                                                </husk:SwitchContainer>
                                            </StackPanel>
                                            <Border
                                                Padding="12,8"
                                                Background="{StaticResource ControlTranslucentFullBackgroundBrush}"
                                                CornerRadius="{StaticResource SmallCornerRadius}">
                                                <TextBlock
                                                    Text="{Binding Description}"
                                                    TextWrapping="Wrap" />
                                            </Border>
                                        </StackPanel>
                                        <Grid
                                            ColumnDefinitions="Auto,*,Auto"
                                            ColumnSpacing="12"
                                            RowDefinitions="Auto,Auto"
                                            RowSpacing="2">
                                            <Border
                                                Grid.RowSpan="2"
                                                Grid.Column="0"
                                                Width="32"
                                                Height="32"
                                                CornerRadius="{StaticResource SmallCornerRadius}">
                                                <Border.Background>
                                                    <ImageBrush
                                                        Source="{Binding Icon}"
                                                        Stretch="Uniform" />
                                                </Border.Background>
                                            </Border>
                                            <TextBlock
                                                Grid.Row="0"
                                                Grid.Column="1"
                                                FontWeight="{StaticResource ControlStrongFontWeight}"
                                                Text="{Binding DisplayName}"
                                                TextTrimming="CharacterEllipsis" />
                                            <TextBlock
                                                Grid.Row="1"
                                                Grid.Column="1"
                                                FontSize="{StaticResource SmallFontSize}"
                                                Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                Text="{Binding Address}"
                                                TextTrimming="CharacterEllipsis" />
                                            <husk:SwitchContainer
                                                Grid.Row="0"
                                                Grid.RowSpan="2"
                                                Grid.Column="2"
                                                TargetType="x:Boolean"
                                                Value="{Binding IsStatusLoading}">
                                                <husk:SwitchCase Value="{x:True}">
                                                    <husk:Tag Content="{x:Static lang:Resources.InstanceFilesPage_ServerCheckingText}" />
                                                </husk:SwitchCase>
                                                <husk:SwitchCase Value="{x:False}">
                                                    <husk:SwitchContainer
                                                        TargetType="x:Boolean"
                                                        Value="{Binding LatencyMilliseconds, Converter={x:Static ObjectConverters.IsNotNull}}">
                                                        <husk:SwitchCase Value="{x:True}">
                                                            <husk:Tag
                                                                Classes="Success"
                                                                Content="{Binding LatencyMilliseconds, StringFormat={}{0}ms}" />
                                                        </husk:SwitchCase>
                                                        <husk:SwitchCase Value="{x:False}">
                                                            <husk:Tag
                                                                Classes="Danger"
                                                                Content="{x:Static lang:Resources.InstanceFilesPage_ServerUnavailableText}" />
                                                        </husk:SwitchCase>
                                                    </husk:SwitchContainer>
                                                </husk:SwitchCase>
                                            </husk:SwitchContainer>
                                        </Grid>
                                    </DockPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </app:EmptyContainer>
                </ScrollViewer>
            </Grid>
        </TabItem>
        <TabItem>
            <TabItem.Header>
                <StackPanel
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock Text="{x:Static lang:Resources.AssetKind_ResourcePack}" />
                    <husk:PlaceholderContainer Source="{Binding ResourcePacks}">
                        <husk:PlaceholderContainer.SourceTemplate>
                            <DataTemplate DataType="collections:IList">
                                <TextBlock Text="{Binding Count, StringFormat={}({0}), FallbackValue=(0)}" />
                            </DataTemplate>
                        </husk:PlaceholderContainer.SourceTemplate>
                        <husk:ProgressRing
                            Width="{Binding $parent[TabItem].FontSize}"
                            Height="{Binding $parent[TabItem].FontSize}"
                            IsIndeterminate="{Binding ResourcePacks, Converter={x:Static ObjectConverters.IsNull}}" />
                    </husk:PlaceholderContainer>
                </StackPanel>
            </TabItem.Header>
            <Grid
                ColumnDefinitions="*,4,*"
                ColumnSpacing="12"
                RowDefinitions="*">
                <!--  左侧：资源包列表  -->
                <Grid
                    Grid.Column="0"
                    RowDefinitions="Auto,*"
                    RowSpacing="8">
                    <!--  搜索栏  -->
                    <TextBox
                        Grid.Row="0"
                        PlaceholderText="Search resource packs..."
                        Text="{Binding ResourcePackSearchText, Mode=TwoWay}">
                        <TextBox.InnerRightContent>
                            <Button
                                IsVisible="{Binding $parent[TextBox].Text, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
                                Command="{Binding $parent[TextBox].Clear}"
                                Content="{fie:SymbolIcon Symbol=Dismiss,
                                                         FontSize={StaticResource MediumFontSize}}"
                                Foreground="{StaticResource ControlSecondaryForegroundBrush}" />
                        </TextBox.InnerRightContent>
                    </TextBox>

                    <!--  资源包列表  -->
                    <ScrollViewer Grid.Row="1">
                        <app:EmptyContainer IsEmpty="{Binding ResourcePacks.Count, Converter={x:Static husk:NumberConverters.IsZero}, FallbackValue=True}">

                            <!--  资源包列表  -->
                            <ListBox
                                ItemsSource="{Binding ResourcePacks}"
                                SelectedItem="{Binding SelectedResourcePack, Mode=TwoWay}"
                                SelectionMode="Single"
                                Theme="{StaticResource CleanListBoxTheme}">
                                <ListBox.ItemTemplate>
                                    <DataTemplate DataType="app:AssetResourcePackModel">
                                        <Grid
                                            ColumnDefinitions="Auto,*"
                                            ColumnSpacing="12">
                                            <!--  资源包图标  -->
                                            <Border
                                                Grid.Column="0"
                                                Width="48"
                                                Height="48"
                                                CornerRadius="{StaticResource SmallCornerRadius}">
                                                <Border.Background>
                                                    <ImageBrush
                                                        Source="{Binding Icon}"
                                                        Stretch="Uniform" />
                                                </Border.Background>
                                            </Border>

                                            <!--  资源包信息  -->
                                            <StackPanel
                                                Grid.Column="1"
                                                VerticalAlignment="Center">
                                                <DockPanel>

                                                    <!--  状态指示器  -->
                                                    <husk:SwitchContainer
                                                        DockPanel.Dock="Right"
                                                        Width="8"
                                                        Height="8"
                                                        VerticalAlignment="Top"
                                                        TargetType="x:Boolean"
                                                        Value="{Binding IsLocked}">
                                                        <husk:SwitchCase Value="{x:False}">
                                                            <husk:SwitchContainer
                                                                Width="8"
                                                                Height="8"
                                                                TargetType="x:Boolean"
                                                                Value="{Binding IsEnabled}">
                                                                <husk:SwitchCase Value="{x:True}">
                                                                    <Border
                                                                        Background="{StaticResource ControlSuccessBackgroundBrush}"
                                                                        CornerRadius="4" />
                                                                </husk:SwitchCase>
                                                                <husk:SwitchCase Value="{x:False}">
                                                                    <Border
                                                                        Background="{StaticResource ControlDangerBackgroundBrush}"
                                                                        CornerRadius="4" />
                                                                </husk:SwitchCase>
                                                            </husk:SwitchContainer>
                                                        </husk:SwitchCase>
                                                        <husk:SwitchCase Value="{x:True}">
                                                            <Border
                                                                Background="{StaticResource ControlWarningBackgroundBrush}"
                                                                CornerRadius="4" />
                                                        </husk:SwitchCase>
                                                    </husk:SwitchContainer>

                                                    <!--  资源包名  -->
                                                    <TextBlock
                                                        FontWeight="{StaticResource ControlStrongFontWeight}"
                                                        Text="{Binding DisplayName}"
                                                        TextTrimming="CharacterEllipsis" />
                                                </DockPanel>

                                                <!--  描述  -->
                                                <TextBlock
                                                    FontSize="{StaticResource SmallFontSize}"
                                                    Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                    MaxLines="1"
                                                    Text="{Binding Description}"
                                                    TextTrimming="CharacterEllipsis" />

                                                <DockPanel>

                                                    <!--  包格式版本  -->
                                                    <husk:Tag
                                                        DockPanel.Dock="Right"
                                                        Classes="Small"
                                                        Content="{Binding PackFormat, StringFormat='Pack Format: {0}'}" />

                                                    <!--  文件大小  -->
                                                    <TextBlock
                                                        FontSize="{StaticResource SmallFontSize}"
                                                        Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                        Text="{Binding FileSize}" />
                                                </DockPanel>
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </app:EmptyContainer>
                    </ScrollViewer>
                </Grid>

                <!--  分隔线  -->
                <GridSplitter
                    Grid.Column="1"
                    Width="2"
                    ResizeDirection="Columns" />

                <!--  右侧：详细信息面板  -->
                <ScrollViewer Grid.Column="2">
                    <husk:PlaceholderContainer Source="{Binding SelectedResourcePack}">
                        <husk:PlaceholderContainer.SourceTemplate>
                            <DataTemplate DataType="app:AssetResourcePackModel">
                                <StackPanel Spacing="8">
                                    <!--  资源包头部  -->
                                    <Grid
                                        ColumnDefinitions="Auto,*"
                                        ColumnSpacing="8">
                                        <Border
                                            Grid.Column="0"
                                            Width="82"
                                            Height="82"
                                            CornerRadius="{StaticResource MediumCornerRadius}">
                                            <Border.Background>
                                                <ImageBrush
                                                    Source="{Binding Icon}"
                                                    Stretch="Uniform" />
                                            </Border.Background>
                                        </Border>
                                        <StackPanel
                                            Grid.Column="1"
                                            VerticalAlignment="Center"
                                            Spacing="4">
                                            <TextBlock
                                                FontSize="{StaticResource ExtraLargeFontSize}"
                                                FontWeight="{StaticResource ControlStrongFontWeight}"
                                                Text="{Binding DisplayName}"
                                                TextTrimming="CharacterEllipsis" />
                                            <StackPanel
                                                Orientation="Horizontal"
                                                Spacing="4">
                                                <husk:Tag
                                                    Classes="Primary"
                                                    IsVisible="{Binding IsLocked}">
                                                    <husk:IconLabel
                                                        Icon="LockClosed"
                                                        Text="{x:Static lang:Resources.InstancePackageButton_ImportTagText}" />
                                                </husk:Tag>
                                                <husk:Tag Content="{Binding PackFormat, StringFormat='Format {0}'}" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Grid>

                                    <!--  操作按钮  -->
                                    <husk:ButtonGroup>
                                        <Button
                                            Command="{Binding $parent[app:InstanceFilesPage].((app:InstanceFilesPageModel)DataContext).ToggleResourcePackCommand, FallbackValue={x:Null}}"
                                            CommandParameter="{Binding}">
                                            <husk:SwitchContainer
                                                TargetType="x:Boolean"
                                                Value="{Binding IsEnabled}">
                                                <husk:SwitchCase Value="{x:True}">
                                                    <husk:IconLabel
                                                        Icon="Power"
                                                        Text="{x:Static lang:Resources.Enum_Disabled}" />
                                                </husk:SwitchCase>
                                                <husk:SwitchCase Value="{x:False}">
                                                    <husk:IconLabel
                                                        Icon="Power"
                                                        Text="{x:Static lang:Resources.Enum_Enabled}" />
                                                </husk:SwitchCase>
                                            </husk:SwitchContainer>
                                        </Button>
                                        <Button
                                            Command="{Binding $parent[app:InstanceFilesPage].((app:InstanceFilesPageModel)DataContext).OpenFolderCommand, FallbackValue={x:Null}}"
                                            CommandParameter="{Binding FilePath}">
                                            <fi:SymbolIcon
                                                FontSize="{StaticResource MediumFontSize}"
                                                Symbol="FolderOpen" />
                                        </Button>
                                        <Button
                                            Command="{Binding $parent[app:InstanceFilesPage].((app:InstanceFilesPageModel)DataContext).DeleteResourcePackCommand, FallbackValue={x:Null}}"
                                            CommandParameter="{Binding}">
                                            <fi:SymbolIcon
                                                FontSize="{StaticResource MediumFontSize}"
                                                Foreground="{StaticResource ControlDangerForegroundBrush}"
                                                Symbol="Delete" />
                                        </Button>
                                    </husk:ButtonGroup>

                                    <!--  资源包描述  -->
                                    <husk:Card
                                        IsVisible="{Binding Metadata.Description, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
                                        BorderThickness="0">
                                        <StackPanel Spacing="8">
                                            <TextBlock
                                                FontWeight="{StaticResource ControlStrongFontWeight}"
                                                Text="{x:Static lang:Resources.InstanceFilesPage_DescriptionLabelText}" />
                                            <TextBlock
                                                Text="{Binding Metadata.Description, FallbackValue=No description available}"
                                                TextWrapping="Wrap" />
                                        </StackPanel>
                                    </husk:Card>

                                    <!--  详细信息  -->
                                    <husk:Card BorderThickness="0">
                                        <StackPanel Spacing="12">
                                            <TextBlock
                                                FontWeight="{StaticResource ControlStrongFontWeight}"
                                                Text="{x:Static lang:Resources.InstanceFilesPage_DetailsLabelText}" />
                                            <Grid
                                                ColumnDefinitions="Auto,*"
                                                ColumnSpacing="16"
                                                RowDefinitions="Auto,Auto,Auto,Auto,Auto"
                                                RowSpacing="8">
                                                <TextBlock
                                                    Grid.Row="0"
                                                    Grid.Column="0"
                                                    Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                    Text="{x:Static lang:Resources.InstanceFilesPage_AssetFileNameLabelText}" />
                                                <TextBlock
                                                    Grid.Row="0"
                                                    Grid.Column="1"
                                                    Text="{Binding FileName, Mode=OneWay}"
                                                    TextWrapping="Wrap" />

                                                <TextBlock
                                                    Grid.Row="1"
                                                    Grid.Column="0"
                                                    Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                    Text="{x:Static lang:Resources.InstanceFilesPage_AssetPackFormatLabelText}" />
                                                <TextBlock
                                                    Grid.Row="1"
                                                    Grid.Column="1"
                                                    Text="{Binding PackFormat}" />

                                                <TextBlock
                                                    Grid.Row="2"
                                                    Grid.Column="0"
                                                    Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                    Text="{x:Static lang:Resources.InstanceFilesPage_AssetFileSizeLabelText}" />
                                                <TextBlock
                                                    Grid.Row="2"
                                                    Grid.Column="1"
                                                    Text="{Binding FileSize}" />

                                                <TextBlock
                                                    Grid.Row="3"
                                                    Grid.Column="0"
                                                    Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                    Text="{x:Static lang:Resources.InstanceFilesPage_AssetLastModifiedLabelText}" />
                                                <TextBlock
                                                    Grid.Row="3"
                                                    Grid.Column="1"
                                                    Text="{Binding LastModifiedFormatted}" />

                                                <TextBlock
                                                    Grid.Row="4"
                                                    Grid.Column="0"
                                                    Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                    Text="{x:Static lang:Resources.InstanceFilesPage_AssetStatusLabelText}" />
                                                <husk:SwitchContainer
                                                    Grid.Row="4"
                                                    Grid.Column="1"
                                                    TargetType="x:Boolean"
                                                    Value="{Binding IsEnabled}">
                                                    <husk:SwitchCase Value="{x:True}">
                                                        <TextBlock
                                                            Foreground="{StaticResource ControlSuccessForegroundBrush}"
                                                            Text="{x:Static lang:Resources.Enum_Enabled}" />
                                                    </husk:SwitchCase>
                                                    <husk:SwitchCase Value="{x:False}">
                                                        <TextBlock
                                                            Foreground="{StaticResource ControlDangerForegroundBrush}"
                                                            Text="{x:Static lang:Resources.Enum_Disabled}" />
                                                    </husk:SwitchCase>
                                                </husk:SwitchContainer>
                                            </Grid>
                                        </StackPanel>
                                    </husk:Card>
                                </StackPanel>
                            </DataTemplate>
                        </husk:PlaceholderContainer.SourceTemplate>

                        <!--  空状态  -->
                        <StackPanel
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Spacing="12">
                            <fi:SymbolIcon
                                HorizontalAlignment="Center"
                                FontSize="32"
                                Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                Symbol="Hexagon" />
                            <TextBlock
                                FontSize="{StaticResource LargeFontSize}"
                                Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                Text="{x:Static lang:Resources.InstanceFilesPage_AssetEmptyPrompt}" />
                        </StackPanel>
                    </husk:PlaceholderContainer>
                </ScrollViewer>
            </Grid>
        </TabItem>
        <TabItem>
            <TabItem.Header>
                <StackPanel
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock Text="{x:Static lang:Resources.AssetKind_DataPack}" />
                    <husk:PlaceholderContainer Source="{Binding DataPacks}">
                        <husk:PlaceholderContainer.SourceTemplate>
                            <DataTemplate DataType="collections:IList">
                                <TextBlock Text="{Binding Count, StringFormat={}({0}), FallbackValue=(0)}" />
                            </DataTemplate>
                        </husk:PlaceholderContainer.SourceTemplate>
                        <husk:ProgressRing
                            Width="{Binding $parent[TabItem].FontSize}"
                            Height="{Binding $parent[TabItem].FontSize}"
                            IsIndeterminate="{Binding DataPacks, Converter={x:Static ObjectConverters.IsNull}}" />
                    </husk:PlaceholderContainer>
                </StackPanel>
            </TabItem.Header>
            <Grid
                ColumnDefinitions="*,4,*"
                ColumnSpacing="12"
                RowDefinitions="*">
                <!--  左侧：数据包列表  -->
                <Grid
                    Grid.Column="0"
                    RowDefinitions="Auto,*"
                    RowSpacing="8">
                    <!--  搜索栏  -->
                    <TextBox
                        Grid.Row="0"
                        PlaceholderText="Search data packs..."
                        Text="{Binding DataPackSearchText, Mode=TwoWay}">
                        <TextBox.InnerRightContent>
                            <Button
                                IsVisible="{Binding $parent[TextBox].Text, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
                                Command="{Binding $parent[TextBox].Clear}"
                                Content="{fie:SymbolIcon Symbol=Dismiss,
                                                         FontSize={StaticResource MediumFontSize}}"
                                Foreground="{StaticResource ControlSecondaryForegroundBrush}" />
                        </TextBox.InnerRightContent>
                    </TextBox>

                    <!--  数据包列表  -->
                    <ScrollViewer Grid.Row="1">
                        <app:EmptyContainer IsEmpty="{Binding DataPacks.Count, Converter={x:Static husk:NumberConverters.IsZero}, FallbackValue=True}">

                            <!--  数据包列表  -->
                            <ListBox
                                ItemsSource="{Binding DataPacks}"
                                SelectedItem="{Binding SelectedDataPack, Mode=TwoWay}"
                                SelectionMode="Single"
                                Theme="{StaticResource CleanListBoxTheme}">
                                <ListBox.ItemTemplate>
                                    <DataTemplate DataType="app:AssetDataPackModel">
                                        <Grid
                                            ColumnDefinitions="Auto,*,Auto"
                                            ColumnSpacing="12">
                                            <!--  数据包图标  -->
                                            <Border
                                                Grid.Column="0"
                                                Width="48"
                                                Height="48"
                                                CornerRadius="{StaticResource SmallCornerRadius}">
                                                <Border.Background>
                                                    <ImageBrush
                                                        Source="{Binding Icon}"
                                                        Stretch="Uniform" />
                                                </Border.Background>
                                            </Border>

                                            <!--  数据包信息  -->
                                            <StackPanel
                                                Grid.Column="1"
                                                VerticalAlignment="Center">

                                                <DockPanel>

                                                    <!--  状态指示器  -->
                                                    <husk:SwitchContainer
                                                        DockPanel.Dock="Right"
                                                        Width="8"
                                                        Height="8"
                                                        VerticalAlignment="Top"
                                                        TargetType="x:Boolean"
                                                        Value="{Binding IsLocked}">
                                                        <husk:SwitchCase Value="{x:False}">
                                                            <husk:SwitchContainer
                                                                Width="8"
                                                                Height="8"
                                                                TargetType="x:Boolean"
                                                                Value="{Binding IsEnabled}">
                                                                <husk:SwitchCase Value="{x:True}">
                                                                    <Border
                                                                        Background="{StaticResource ControlSuccessBackgroundBrush}"
                                                                        CornerRadius="4" />
                                                                </husk:SwitchCase>
                                                                <husk:SwitchCase Value="{x:False}">
                                                                    <Border
                                                                        Background="{StaticResource ControlDangerBackgroundBrush}"
                                                                        CornerRadius="4" />
                                                                </husk:SwitchCase>
                                                            </husk:SwitchContainer>
                                                        </husk:SwitchCase>
                                                        <husk:SwitchCase Value="{x:True}">
                                                            <Border
                                                                Background="{StaticResource ControlWarningBackgroundBrush}"
                                                                CornerRadius="4" />
                                                        </husk:SwitchCase>
                                                    </husk:SwitchContainer>

                                                    <!--  数据包名称  -->
                                                    <TextBlock
                                                        FontWeight="{StaticResource ControlStrongFontWeight}"
                                                        Text="{Binding DisplayName}"
                                                        TextTrimming="CharacterEllipsis" />
                                                </DockPanel>

                                                <!--  描述  -->
                                                <TextBlock
                                                    FontSize="{StaticResource SmallFontSize}"
                                                    Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                    MaxLines="1"
                                                    Text="{Binding Description}"
                                                    TextTrimming="CharacterEllipsis" />

                                                <DockPanel>

                                                    <!--  包格式版本  -->
                                                    <husk:Tag
                                                        DockPanel.Dock="Right"
                                                        Classes="Small"
                                                        Content="{Binding PackFormat, StringFormat='Pack Format: {0}'}" />

                                                    <!--  文件大小  -->
                                                    <TextBlock
                                                        FontSize="{StaticResource SmallFontSize}"
                                                        Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                        Text="{Binding FileSize}" />
                                                </DockPanel>
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </app:EmptyContainer>
                    </ScrollViewer>
                </Grid>

                <!--  分隔线  -->
                <GridSplitter
                    Grid.Column="1"
                    Width="2"
                    ResizeDirection="Columns" />

                <!--  右侧：详细信息面板  -->
                <ScrollViewer Grid.Column="2">
                    <husk:PlaceholderContainer Source="{Binding SelectedDataPack}">
                        <husk:PlaceholderContainer.SourceTemplate>
                            <DataTemplate DataType="app:AssetDataPackModel">
                                <StackPanel Spacing="8">
                                    <!--  数据包头部  -->
                                    <Grid
                                        ColumnDefinitions="Auto,*"
                                        ColumnSpacing="8">
                                        <Border
                                            Grid.Column="0"
                                            Width="82"
                                            Height="82"
                                            CornerRadius="{StaticResource MediumCornerRadius}">
                                            <Border.Background>
                                                <ImageBrush
                                                    Source="{Binding Icon}"
                                                    Stretch="Uniform" />
                                            </Border.Background>
                                        </Border>
                                        <StackPanel
                                            Grid.Column="1"
                                            VerticalAlignment="Center"
                                            Spacing="4">
                                            <TextBlock
                                                FontSize="{StaticResource ExtraLargeFontSize}"
                                                FontWeight="{StaticResource ControlStrongFontWeight}"
                                                Text="{Binding DisplayName}"
                                                TextTrimming="CharacterEllipsis" />
                                            <StackPanel
                                                Orientation="Horizontal"
                                                Spacing="4">
                                                <husk:Tag
                                                    Classes="Primary"
                                                    IsVisible="{Binding IsLocked}">
                                                    <husk:IconLabel
                                                        Icon="LockClosed"
                                                        Text="{x:Static lang:Resources.InstancePackageButton_ImportTagText}" />
                                                </husk:Tag>
                                                <husk:Tag Content="{Binding PackFormat, StringFormat='Format {0}'}" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Grid>

                                    <!--  操作按钮  -->
                                    <husk:ButtonGroup>
                                        <Button
                                            Command="{Binding $parent[app:InstanceFilesPage].((app:InstanceFilesPageModel)DataContext).ToggleDataPackCommand, FallbackValue={x:Null}}"
                                            CommandParameter="{Binding}">
                                            <husk:SwitchContainer
                                                TargetType="x:Boolean"
                                                Value="{Binding IsEnabled}">
                                                <husk:SwitchCase Value="{x:True}">
                                                    <husk:IconLabel
                                                        Icon="Power"
                                                        Text="{x:Static lang:Resources.Enum_Disabled}" />
                                                </husk:SwitchCase>
                                                <husk:SwitchCase Value="{x:False}">
                                                    <husk:IconLabel
                                                        Icon="Power"
                                                        Text="{x:Static lang:Resources.Enum_Enabled}" />
                                                </husk:SwitchCase>
                                            </husk:SwitchContainer>
                                        </Button>
                                        <Button
                                            Command="{Binding $parent[app:InstanceFilesPage].((app:InstanceFilesPageModel)DataContext).OpenFolderCommand, FallbackValue={x:Null}}"
                                            CommandParameter="{Binding FilePath}">
                                            <fi:SymbolIcon
                                                FontSize="{StaticResource MediumFontSize}"
                                                Symbol="FolderOpen" />
                                        </Button>
                                        <Button
                                            Command="{Binding $parent[app:InstanceFilesPage].((app:InstanceFilesPageModel)DataContext).DeleteDataPackCommand, FallbackValue={x:Null}}"
                                            CommandParameter="{Binding}">
                                            <fi:SymbolIcon
                                                FontSize="{StaticResource MediumFontSize}"
                                                Foreground="{StaticResource ControlDangerForegroundBrush}"
                                                Symbol="Delete" />
                                        </Button>
                                    </husk:ButtonGroup>

                                    <!--  数据包描述  -->
                                    <Border
                                        Padding="16"
                                        IsVisible="{Binding Metadata.Description, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
                                        Background="{StaticResource CardBackgroundBrush}"
                                        CornerRadius="{StaticResource MediumCornerRadius}">
                                        <StackPanel Spacing="8">
                                            <TextBlock
                                                FontWeight="{StaticResource ControlStrongFontWeight}"
                                                Text="Description" />
                                            <TextBlock
                                                Text="{Binding Metadata.Description, FallbackValue=No description available}"
                                                TextWrapping="Wrap" />
                                        </StackPanel>
                                    </Border>

                                    <!--  详细信息  -->
                                    <Border
                                        Padding="16"
                                        Background="{StaticResource CardBackgroundBrush}"
                                        CornerRadius="{StaticResource MediumCornerRadius}">
                                        <StackPanel Spacing="12">
                                            <TextBlock
                                                FontWeight="{StaticResource ControlStrongFontWeight}"
                                                Text="Details" />
                                            <Grid
                                                ColumnDefinitions="Auto,*"
                                                ColumnSpacing="16"
                                                RowDefinitions="Auto,Auto,Auto,Auto,Auto"
                                                RowSpacing="8">
                                                <TextBlock
                                                    Grid.Row="0"
                                                    Grid.Column="0"
                                                    Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                    Text="File Name:" />
                                                <TextBlock
                                                    Grid.Row="0"
                                                    Grid.Column="1"
                                                    Text="{Binding FileName, Mode=OneWay}"
                                                    TextWrapping="Wrap" />

                                                <TextBlock
                                                    Grid.Row="1"
                                                    Grid.Column="0"
                                                    Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                    Text="Pack Format:" />
                                                <TextBlock
                                                    Grid.Row="1"
                                                    Grid.Column="1"
                                                    Text="{Binding PackFormat}" />

                                                <TextBlock
                                                    Grid.Row="2"
                                                    Grid.Column="0"
                                                    Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                    Text="File Size:" />
                                                <TextBlock
                                                    Grid.Row="2"
                                                    Grid.Column="1"
                                                    Text="{Binding FileSize}" />

                                                <TextBlock
                                                    Grid.Row="3"
                                                    Grid.Column="0"
                                                    Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                    Text="Last Modified:" />
                                                <TextBlock
                                                    Grid.Row="3"
                                                    Grid.Column="1"
                                                    Text="{Binding LastModifiedFormatted}" />

                                                <TextBlock
                                                    Grid.Row="4"
                                                    Grid.Column="0"
                                                    Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                    Text="Status:" />
                                                <husk:SwitchContainer
                                                    Grid.Row="4"
                                                    Grid.Column="1"
                                                    TargetType="x:Boolean"
                                                    Value="{Binding IsEnabled}">
                                                    <husk:SwitchCase Value="{x:True}">
                                                        <TextBlock
                                                            Foreground="{StaticResource ControlSuccessForegroundBrush}"
                                                            Text="{x:Static lang:Resources.Enum_Enabled}" />
                                                    </husk:SwitchCase>
                                                    <husk:SwitchCase Value="{x:False}">
                                                        <TextBlock
                                                            Foreground="{StaticResource ControlDangerForegroundBrush}"
                                                            Text="{x:Static lang:Resources.Enum_Disabled}" />
                                                    </husk:SwitchCase>
                                                </husk:SwitchContainer>
                                            </Grid>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </DataTemplate>
                        </husk:PlaceholderContainer.SourceTemplate>

                        <!--  空状态  -->
                        <StackPanel
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Spacing="12">
                            <fi:SymbolIcon
                                HorizontalAlignment="Center"
                                FontSize="32"
                                Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                Symbol="Hexagon" />
                            <TextBlock
                                FontSize="{StaticResource LargeFontSize}"
                                Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                Text="{x:Static lang:Resources.InstanceFilesPage_AssetEmptyPrompt}" />
                        </StackPanel>
                    </husk:PlaceholderContainer>
                </ScrollViewer>
            </Grid>
        </TabItem>
        <TabItem>
            <TabItem.Header>
                <StackPanel
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock Text="{x:Static lang:Resources.AssetKind_Screenshot}" />
                    <husk:PlaceholderContainer Source="{Binding ScreenshotGroups}">
                        <husk:PlaceholderContainer.SourceTemplate>
                            <DataTemplate DataType="app:AssetScreenshotCollection">
                                <TextBlock Text="{Binding ScreenshotCount, StringFormat={}({0}), FallbackValue=(0)}" />
                            </DataTemplate>
                        </husk:PlaceholderContainer.SourceTemplate>
                        <husk:ProgressRing
                            Width="{Binding $parent[TabItem].FontSize}"
                            Height="{Binding $parent[TabItem].FontSize}"
                            IsIndeterminate="{Binding ScreenshotGroups, Converter={x:Static ObjectConverters.IsNull}}" />
                    </husk:PlaceholderContainer>
                </StackPanel>
            </TabItem.Header>
            <Panel>
                <StackPanel
                    VerticalAlignment="Center"
                    IsVisible="{Binding ScreenshotGroups.Count, Converter={x:Static husk:NumberConverters.IsZero}, FallbackValue=True}"
                    Spacing="8">
                    <fi:SymbolIcon
                        Height="{StaticResource ExtraLargeFontSize}"
                        HorizontalAlignment="Center"
                        FontSize="{StaticResource ExtraLargeFontSize}"
                        Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                        Symbol="Image" />
                    <TextBlock
                        HorizontalAlignment="Center"
                        FontSize="{StaticResource LargeFontSize}"
                        Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                        Text="{x:Static lang:Resources.Shared_EmptyListLabelText}" />
                </StackPanel>
                <ScrollViewer
                    HorizontalScrollBarVisibility="Auto"
                    VerticalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding ScreenshotGroups}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel
                                    Orientation="Horizontal"
                                    Spacing="12" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="app:AssetScreenshotGroupModel">
                                <Border
                                    Width="256"
                                    Margin="2"
                                    VerticalAlignment="Top"
                                    Background="{StaticResource CardBackgroundBrush}"
                                    BoxShadow="0 0 4 0 #3F000000"
                                    CornerRadius="{StaticResource MediumCornerRadius}">
                                    <DockPanel>
                                        <TextBlock
                                            DockPanel.Dock="Top"
                                            Margin="12,12,12,0"
                                            Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                            Text="{Binding Date}" />
                                        <ScrollViewer>
                                            <ItemsControl
                                                Margin="12"
                                                ItemsSource="{Binding Screenshots}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <StackPanel Spacing="8" />
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate DataType="app:AssetScreenshotModel">
                                                        <Border
                                                            Height="178"
                                                            Padding="8"
                                                            Background="{StaticResource OverlaySolidBackgroundBrush}"
                                                            BorderBrush="{StaticResource ControlBorderBrush}"
                                                            BorderThickness="1"
                                                            CornerRadius="{StaticResource MediumCornerRadius}">
                                                            <Grid
                                                                RowDefinitions="*,Auto"
                                                                RowSpacing="8">
                                                                <Button
                                                                    Grid.Row="0"
                                                                    Command="{Binding $parent[app:InstanceFilesPage].((app:InstanceFilesPageModel)DataContext).ViewImageCommand, FallbackValue={x:Null}}"
                                                                    CommandParameter="{Binding}"
                                                                    RenderOptions.BitmapInterpolationMode="LowQuality">
                                                                    <Button.Theme>
                                                                        <ControlTheme TargetType="Button">
                                                                            <Setter Property="Cursor" Value="Hand" />
                                                                            <Setter Property="CornerRadius" Value="{StaticResource SmallCornerRadius}" />
                                                                            <Setter Property="Template">
                                                                                <ControlTemplate>
                                                                                    <Panel>
                                                                                        <Border
                                                                                            Background="{TemplateBinding Background}"
                                                                                            CornerRadius="{TemplateBinding CornerRadius}" />
                                                                                        <Border
                                                                                            Name="Mask"
                                                                                            Background="{StaticResource OverlaySmokeBackgroundBrush}"
                                                                                            CornerRadius="{TemplateBinding CornerRadius}"
                                                                                            Opacity="0">
                                                                                            <Border.Transitions>
                                                                                                <Transitions>
                                                                                                    <DoubleTransition
                                                                                                        Easing="SineEaseOut"
                                                                                                        Property="Opacity"
                                                                                                        Duration="{StaticResource ControlFasterAnimationDuration}" />
                                                                                                </Transitions>
                                                                                            </Border.Transitions>
                                                                                            <fi:SymbolIcon
                                                                                                HorizontalAlignment="Center"
                                                                                                VerticalAlignment="Center"
                                                                                                FontSize="36"
                                                                                                Foreground="{StaticResource ControlWhiteForegroundBrush}"
                                                                                                Symbol="ArrowExpand" />
                                                                                        </Border>
                                                                                    </Panel>
                                                                                </ControlTemplate>
                                                                            </Setter>

                                                                            <Style Selector="^:pointerover /template/ Border#Mask">
                                                                                <Setter Property="Opacity" Value="0.88" />
                                                                            </Style>
                                                                        </ControlTheme>
                                                                    </Button.Theme>
                                                                    <Button.Background>
                                                                        <ImageBrush
                                                                            Source="{Binding Thumbnail}"
                                                                            Stretch="UniformToFill" />
                                                                    </Button.Background>
                                                                </Button>
                                                                <DockPanel Grid.Row="1">
                                                                    <husk:ButtonGroup DockPanel.Dock="Right">
                                                                        <Button
                                                                            Command="{Binding $parent[app:InstanceFilesPage].((app:InstanceFilesPageModel)DataContext).OpenScreenshotFileCommand, FallbackValue={x:Null}}"
                                                                            CommandParameter="{Binding}">
                                                                            <fi:SymbolIcon
                                                                                FontSize="{StaticResource SmallFontSize}"
                                                                                Symbol="DocumentImage" />
                                                                        </Button>
                                                                        <Button
                                                                            Command="{Binding $parent[app:InstanceFilesPage].((app:InstanceFilesPageModel)DataContext).OpenFolderCommand, FallbackValue={x:Null}}"
                                                                            CommandParameter="{Binding Image.LocalPath}">
                                                                            <fi:SymbolIcon
                                                                                FontSize="{StaticResource SmallFontSize}"
                                                                                Symbol="FolderOpen" />
                                                                        </Button>
                                                                        <Button
                                                                            Command="{Binding $parent[app:InstanceFilesPage].((app:InstanceFilesPageModel)DataContext).DeleteScreenshotCommand, FallbackValue={x:Null}}"
                                                                            CommandParameter="{Binding}">
                                                                            <fi:SymbolIcon
                                                                                FontSize="{StaticResource SmallFontSize}"
                                                                                Foreground="{StaticResource ControlDangerForegroundBrush}"
                                                                                Symbol="Delete" />
                                                                        </Button>
                                                                    </husk:ButtonGroup>
                                                                    <StackPanel
                                                                        Orientation="Horizontal"
                                                                        Spacing="4">
                                                                        <husk:Tag
                                                                            Classes="Primary"
                                                                            IsVisible="{Binding IsLocked}">
                                                                            <husk:IconLabel
                                                                                Icon="LockClosed"
                                                                                Text="{x:Static lang:Resources.InstancePackageButton_ImportTagText}" />
                                                                        </husk:Tag>
                                                                        <husk:Tag Content="{Binding Time}" />
                                                                    </StackPanel>
                                                                </DockPanel>
                                                            </Grid>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </ScrollViewer>
                                    </DockPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Panel>
        </TabItem>
        <TabItem>
            <TabItem.Header>
                <StackPanel
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock Text="{x:Static lang:Resources.AssetKind_Save}" />
                    <husk:PlaceholderContainer Source="{Binding Worlds}">
                        <husk:PlaceholderContainer.SourceTemplate>
                            <DataTemplate DataType="collections:IList">
                                <TextBlock Text="{Binding Count, StringFormat={}({0}), FallbackValue=(0)}" />
                            </DataTemplate>
                        </husk:PlaceholderContainer.SourceTemplate>
                        <husk:ProgressRing
                            Width="{Binding $parent[TabItem].FontSize}"
                            Height="{Binding $parent[TabItem].FontSize}"
                            IsIndeterminate="{Binding Worlds, Converter={x:Static ObjectConverters.IsNull}}" />
                    </husk:PlaceholderContainer>
                </StackPanel>
            </TabItem.Header>
            <Grid
                RowDefinitions="Auto,*"
                RowSpacing="12">
                <!--  存档列表  -->
                <ScrollViewer
                    Grid.Row="0"
                    HorizontalScrollBarVisibility="Auto"
                    VerticalScrollBarVisibility="Disabled">
                    <TabStrip
                        ItemsSource="{Binding Worlds}"
                        SelectedItem="{Binding SelectedWorld, Mode=TwoWay}">
                        <TabStrip.ItemTemplate>
                            <DataTemplate DataType="app:AssetWorldModel">
                                <Grid
                                    ColumnDefinitions="Auto,*"
                                    ColumnSpacing="6">
                                    <Border
                                        Grid.Column="0"
                                        Width="42"
                                        Height="42"
                                        CornerRadius="{StaticResource SmallCornerRadius}">
                                        <Border.Background>
                                            <ImageBrush
                                                Source="{Binding Icon}"
                                                Stretch="Uniform" />
                                        </Border.Background>
                                    </Border>
                                    <StackPanel Grid.Column="1">
                                        <TextBlock
                                            FontSize="{StaticResource LargeFontSize}"
                                            FontWeight="{StaticResource ControlStrongFontWeight}"
                                            Text="{Binding DisplayName}"
                                            TextTrimming="CharacterEllipsis" />
                                        <TextBlock
                                            Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                            Text="{Binding LastPlayed}" />
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </TabStrip.ItemTemplate>
                    </TabStrip>
                </ScrollViewer>

                <!--  存档详情  -->
                <ScrollViewer Grid.Row="1">
                    <husk:PlaceholderContainer Source="{Binding SelectedWorld}">
                        <husk:PlaceholderContainer.SourceTemplate>
                            <DataTemplate DataType="app:AssetWorldModel">
                                <StackPanel Spacing="12">
                                    <!--  存档头部  -->
                                    <Grid
                                        ColumnDefinitions="Auto,*"
                                        ColumnSpacing="12">
                                        <Border
                                            Grid.Column="0"
                                            Width="82"
                                            Height="82"
                                            CornerRadius="{StaticResource MediumCornerRadius}">
                                            <Border.Background>
                                                <ImageBrush
                                                    Source="{Binding Icon}"
                                                    Stretch="Uniform" />
                                            </Border.Background>
                                        </Border>
                                        <StackPanel
                                            Grid.Column="1"
                                            VerticalAlignment="Center"
                                            Spacing="6">
                                            <TextBlock
                                                FontSize="{StaticResource ExtraLargeFontSize}"
                                                FontWeight="{StaticResource ControlStrongFontWeight}"
                                                Text="{Binding DisplayName}" />
                                            <StackPanel
                                                Orientation="Horizontal"
                                                Spacing="4">
                                                <husk:Tag Content="{Binding Version}" />
                                                <husk:Tag Content="{Binding GameMode}" />
                                                <husk:Tag Content="{Binding Difficulty}" />
                                                <husk:Tag
                                                    Classes="Danger"
                                                    IsVisible="{Binding Hardcore}"
                                                    Content="Hardcore" />
                                            </StackPanel>
                                            <TextBlock>
                                                <Run
                                                    Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                    Text="Last Played:" />
                                                <Run Text="{Binding LastPlayedFormatted}" />
                                            </TextBlock>
                                        </StackPanel>
                                    </Grid>

                                    <!--  操作按钮  -->
                                    <husk:ButtonGroup>
                                        <!--  <Button  -->
                                        <!--  Command="{Binding $parent[v:InstanceFilesPage].((vm:InstanceFilesPageModel)DataContext).OpenFolderCommand,FallbackValue={x:Null}}"  -->
                                        <!--  CommandParameter="{Binding WorldPath}">  -->
                                        <!--     <husk:IconLabel Icon="Archive" Text="Export" /> -->
                                        <!-- </Button> -->
                                        <Button
                                            Command="{Binding $parent[app:InstanceFilesPage].((app:InstanceFilesPageModel)DataContext).OpenFolderCommand, FallbackValue={x:Null}}"
                                            CommandParameter="{Binding WorldPath}">
                                            <fi:SymbolIcon
                                                FontSize="{StaticResource MediumFontSize}"
                                                Symbol="FolderOpen" />
                                        </Button>
                                        <!--  <Button  -->
                                        <!--  Command="{Binding $parent[v:InstanceFilesPage].((vm:InstanceFilesPageModel)DataContext).OpenFolderCommand,FallbackValue={x:Null}}"  -->
                                        <!--  CommandParameter="{Binding WorldPath}">  -->
                                        <!--  <fi:SymbolIcon Symbol="Delete"  -->
                                        <!--  Foreground="{StaticResource ControlDangerForegroundBrush}"  -->
                                        <!--  FontSize="{StaticResource MediumFontSize}" />  -->
                                        <!-- </Button> -->
                                    </husk:ButtonGroup>

                                    <!--  基本信息  -->
                                    <husk:Card BorderThickness="0">
                                        <StackPanel Spacing="12">
                                            <TextBlock
                                                FontWeight="{StaticResource ControlStrongFontWeight}"
                                                Text="{x:Static lang:Resources.InstanceFilesPage_DetailsLabelText}" />
                                            <Grid
                                                ColumnDefinitions="Auto,*"
                                                ColumnSpacing="16"
                                                RowDefinitions="Auto,Auto,Auto,Auto,Auto"
                                                RowSpacing="8">
                                                <TextBlock
                                                    Grid.Row="0"
                                                    Grid.Column="0"
                                                    Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                    Text="Game Time:" />
                                                <TextBlock
                                                    Grid.Row="0"
                                                    Grid.Column="1">
                                                    <Run Text="{Binding GameTime}" />
                                                    <Run
                                                        Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                        Text="ticks" />
                                                </TextBlock>

                                                <TextBlock
                                                    Grid.Row="1"
                                                    Grid.Column="0"
                                                    Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                    Text="Day:" />
                                                <TextBlock
                                                    Grid.Row="1"
                                                    Grid.Column="1"
                                                    Text="{Binding DayCount}" />

                                                <TextBlock
                                                    Grid.Row="2"
                                                    Grid.Column="0"
                                                    Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                    Text="Day Time:" />
                                                <TextBlock
                                                    Grid.Row="2"
                                                    Grid.Column="1"
                                                    Text="{Binding DayTime}" />

                                                <TextBlock
                                                    Grid.Row="3"
                                                    Grid.Column="0"
                                                    Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                    Text="Commands:" />
                                                <TextBlock
                                                    Grid.Row="3"
                                                    Grid.Column="1"
                                                    Text="{Binding AllowCommands}" />

                                                <TextBlock
                                                    Grid.Row="4"
                                                    Grid.Column="0"
                                                    Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                    Text="Folder:" />
                                                <TextBlock
                                                    Grid.Row="4"
                                                    Grid.Column="1"
                                                    Text="{Binding FolderName}"
                                                    TextWrapping="Wrap" />
                                            </Grid>
                                        </StackPanel>
                                    </husk:Card>

                                    <!--  数据包板块  -->
                                    <Expander
                                        Padding="0"
                                        Header="{x:Static lang:Resources.InstanceFilesPage_DataPacksLabelText}"
                                        IsExpanded="True">
                                        <app:EmptyContainer IsEmpty="{Binding $parent[app:InstanceFilesPage].((app:InstanceFilesPageModel)DataContext).SelectedWorldDataPacks.Count, Converter={x:Static husk:NumberConverters.IsZero}, FallbackValue={x:False}}">
                                            <ScrollViewer MaxHeight="320">
                                                <ItemsControl
                                                    Margin="{StaticResource ExpanderContentMargin}"
                                                    ItemsSource="{Binding $parent[app:InstanceFilesPage].((app:InstanceFilesPageModel)DataContext).SelectedWorldDataPacks, FallbackValue={x:Null}}">
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <StackPanel Spacing="8" />
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate DataType="app:AssetWorldDataPackModel">
                                                            <Grid
                                                                ColumnDefinitions="Auto,*"
                                                                ColumnSpacing="12">
                                                                <!--  数据包图标  -->
                                                                <Border
                                                                    Grid.Column="0"
                                                                    Width="48"
                                                                    Height="48"
                                                                    CornerRadius="{StaticResource SmallCornerRadius}">
                                                                    <Border.Background>
                                                                        <ImageBrush
                                                                            Source="{Binding Icon}"
                                                                            Stretch="Uniform" />
                                                                    </Border.Background>
                                                                </Border>

                                                                <!--  数据包信息  -->
                                                                <StackPanel
                                                                    Grid.Column="1"
                                                                    VerticalAlignment="Center">
                                                                    <DockPanel>

                                                                        <!--  状态指示器  -->
                                                                        <husk:SwitchContainer
                                                                            DockPanel.Dock="Right"
                                                                            Width="8"
                                                                            Height="8"
                                                                            VerticalAlignment="Top"
                                                                            TargetType="x:Boolean"
                                                                            Value="{Binding IsEnabled}">
                                                                            <husk:SwitchCase Value="{x:True}">
                                                                                <Border
                                                                                    Background="{StaticResource ControlSuccessBackgroundBrush}"
                                                                                    CornerRadius="4" />
                                                                            </husk:SwitchCase>
                                                                            <husk:SwitchCase Value="{x:False}">
                                                                                <Border
                                                                                    Background="{StaticResource ControlDangerBackgroundBrush}"
                                                                                    CornerRadius="4" />
                                                                            </husk:SwitchCase>
                                                                        </husk:SwitchContainer>

                                                                        <!--  数据包名称  -->
                                                                        <TextBlock
                                                                            FontWeight="{StaticResource ControlStrongFontWeight}"
                                                                            Text="{Binding DisplayName}"
                                                                            TextTrimming="CharacterEllipsis" />
                                                                    </DockPanel>

                                                                    <!--  描述  -->
                                                                    <TextBlock
                                                                        FontSize="{StaticResource SmallFontSize}"
                                                                        Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                                        MaxLines="1"
                                                                        Text="{Binding DescriptionText}"
                                                                        TextTrimming="CharacterEllipsis" />

                                                                    <DockPanel>
                                                                        <!--  包格式版本  -->
                                                                        <husk:Tag
                                                                            DockPanel.Dock="Right"
                                                                            Classes="Small"
                                                                            Content="{Binding PackFormatText, StringFormat='Format {0}'}" />

                                                                        <!--  文件名  -->
                                                                        <TextBlock
                                                                            FontSize="{StaticResource SmallFontSize}"
                                                                            Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                                            Text="{Binding FileName}"
                                                                            TextTrimming="CharacterEllipsis" />
                                                                    </DockPanel>
                                                                </StackPanel>
                                                            </Grid>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </ScrollViewer>
                                        </app:EmptyContainer>
                                    </Expander>

                                    <!--  玩家板块  -->
                                    <Expander
                                        Header="{x:Static lang:Resources.InstanceFilesPage_PlayersLabelText}"
                                        IsExpanded="True">
                                        <app:EmptyContainer IsEmpty="{Binding $parent[app:InstanceFilesPage].((app:InstanceFilesPageModel)DataContext).SelectedWorldPlayers.Count, Converter={x:Static husk:NumberConverters.IsZero}, FallbackValue={x:False}}">
                                            <Grid
                                                RowDefinitions="Auto,*"
                                                RowSpacing="12">
                                                <!--  玩家头像列表  -->
                                                <TabStrip
                                                    Grid.Row="0"
                                                    ItemsSource="{Binding $parent[app:InstanceFilesPage].((app:InstanceFilesPageModel)DataContext).SelectedWorldPlayers, FallbackValue={x:Null}}"
                                                    SelectedItem="{Binding $parent[app:InstanceFilesPage].((app:InstanceFilesPageModel)DataContext).SelectedWorldPlayer, Mode=TwoWay, FallbackValue={x:Null}}">
                                                    <TabStrip.ItemTemplate>
                                                        <DataTemplate DataType="app:AssetWorldPlayerModel">
                                                            <StackPanel
                                                                Margin="8,4"
                                                                Spacing="4">
                                                                <Border
                                                                    Width="48"
                                                                    Height="48"
                                                                    CornerRadius="{StaticResource SmallCornerRadius}">
                                                                    <Border.Background>
                                                                        <ImageBrush
                                                                            async:ImageBrushLoader.Source="{Binding FaceUrl}"
                                                                            Stretch="Uniform" />
                                                                    </Border.Background>
                                                                </Border>
                                                                <TextBlock
                                                                    HorizontalAlignment="Center"
                                                                    FontSize="{StaticResource SmallFontSize}"
                                                                    Text="{Binding UserName}" />
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </TabStrip.ItemTemplate>
                                                </TabStrip>

                                                <!--  玩家数据  -->
                                                <StackPanel
                                                    Grid.Row="1"
                                                    Spacing="16">
                                                    <!--  统计数据  -->
                                                    <StackPanel Spacing="8">
                                                        <TextBlock
                                                            FontSize="{StaticResource MediumFontSize}"
                                                            FontWeight="{StaticResource ControlStrongFontWeight}"
                                                            Text="Statistics" />
                                                        <ItemsControl ItemsSource="{Binding $parent[app:InstanceFilesPage].((app:InstanceFilesPageModel)DataContext).SelectedPlayerStats, FallbackValue={x:Null}}">
                                                            <ItemsControl.ItemsPanel>
                                                                <ItemsPanelTemplate>
                                                                    <WrapPanel Orientation="Horizontal" />
                                                                </ItemsPanelTemplate>
                                                            </ItemsControl.ItemsPanel>
                                                            <ItemsControl.ItemTemplate>
                                                                <DataTemplate DataType="app:AssetWorldPlayerStatEntryModel">
                                                                    <Border
                                                                        MinWidth="120"
                                                                        Margin="0,0,8,8"
                                                                        Padding="12,8"
                                                                        Background="{StaticResource CardBackgroundBrush}"
                                                                        CornerRadius="{StaticResource MediumCornerRadius}">
                                                                        <StackPanel Spacing="4">
                                                                            <fi:SymbolIcon
                                                                                HorizontalAlignment="Left"
                                                                                FontSize="20"
                                                                                Foreground="{StaticResource ControlAccentForegroundBrush}"
                                                                                Symbol="{Binding Icon}" />
                                                                            <TextBlock
                                                                                FontSize="{StaticResource SmallFontSize}"
                                                                                Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                                                                Text="{Binding Name}" />
                                                                            <TextBlock
                                                                                FontSize="{StaticResource MediumFontSize}"
                                                                                FontWeight="{StaticResource ControlStrongFontWeight}"
                                                                                Text="{Binding Value}" />
                                                                        </StackPanel>
                                                                    </Border>
                                                                </DataTemplate>
                                                            </ItemsControl.ItemTemplate>
                                                        </ItemsControl>
                                                    </StackPanel>
                                                </StackPanel>
                                            </Grid>
                                        </app:EmptyContainer>
                                    </Expander>
                                </StackPanel>
                            </DataTemplate>
                        </husk:PlaceholderContainer.SourceTemplate>

                        <!--  空状态  -->
                        <StackPanel
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Spacing="12">
                            <fi:SymbolIcon
                                HorizontalAlignment="Center"
                                FontSize="32"
                                Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                Symbol="Globe" />
                            <TextBlock
                                FontSize="{StaticResource LargeFontSize}"
                                Foreground="{StaticResource ControlSecondaryForegroundBrush}"
                                Text="{x:Static lang:Resources.Shared_EmptyListLabelText}" />
                        </StackPanel>
                    </husk:PlaceholderContainer>
                </ScrollViewer>
            </Grid>
        </TabItem>
    </TabControl>
</app:Subpage>
