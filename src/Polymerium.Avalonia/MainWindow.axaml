<husk:AppWindow
    x:Class="Polymerium.Avalonia.MainWindow"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:app="https://github.com/d3ara1n/Polymerium"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:fi="clr-namespace:FluentIcons.Avalonia;assembly=FluentIcons.Avalonia"
    xmlns:fie="clr-namespace:FluentIcons.Avalonia.Markup;assembly=FluentIcons.Avalonia"
    xmlns:husk="https://github.com/d3ara1n/Huskui.Avalonia"
    xmlns:lang="https://github.com/d3ara1n/Polymerium/Languages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="{x:Static app:Program.Brand}"
    Width="1111"
    Height="666"
    MinWidth="600"
    MinHeight="460"
    d:DesignHeight="450"
    d:DesignWidth="800"
    x:DataType="app:MainWindowContext"
    Icon="/Assets/Icon.App.ico"
    Loaded="Control_OnLoaded"
    Unloaded="Control_OnUnloaded"
    mc:Ignorable="d">
    <husk:AppWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.ThemeDictionaries>
                <!--  macOS Tahoe 风格侧边栏：Liquid Glass 毛玻璃背景 + 玻璃边缘描边 + 柔和浮起投影  -->
                <ResourceDictionary x:Key="Light">
                    <SolidColorBrush
                        x:Key="SidebarBackground"
                        Color="#bfffffff" />
                    <SolidColorBrush
                        x:Key="SidebarBorder"
                        Color="#14000000" />
                </ResourceDictionary>
                <ResourceDictionary x:Key="Dark" />
            </ResourceDictionary.ThemeDictionaries>
        </ResourceDictionary>
    </husk:AppWindow.Resources>
    <NativeMenu.Menu>
        <NativeMenu>
            <NativeMenuItem Header="{x:Static lang:Resources.AppMenu_FileText}">
                <NativeMenuItem.Menu>
                    <NativeMenu>
                        <NativeMenuItem
                            Command="{Binding NewInstanceCommand}"
                            Gesture="Cmd+N"
                            Header="{x:Static lang:Resources.AppMenu_NewInstanceText}" />
                        <NativeMenuItemSeparator />
                        <NativeMenuItem
                            Command="{Binding GoSettingsCommand}"
                            Gesture="Cmd+,"
                            Header="{x:Static lang:Resources.AppMenu_SettingsText}" />
                    </NativeMenu>
                </NativeMenuItem.Menu>
            </NativeMenuItem>
            <NativeMenuItem Header="{x:Static lang:Resources.AppMenu_ViewText}">
                <NativeMenuItem.Menu>
                    <NativeMenu>
                        <NativeMenuItem
                            Command="{Binding GoHomeCommand}"
                            Header="{x:Static lang:Resources.AppMenu_HomeText}" />
                        <NativeMenuItem
                            Command="{Binding GoMarketplaceCommand}"
                            Header="{x:Static lang:Resources.AppMenu_MarketplaceText}" />
                        <NativeMenuItem
                            Command="{Binding GoRecipesCommand}"
                            Header="{x:Static lang:Resources.AppMenu_RecipesText}" />
                        <NativeMenuItem
                            Command="{Binding GoInstancesCommand}"
                            Header="{x:Static lang:Resources.AppMenu_InstancesText}" />
                        <NativeMenuItem
                            Command="{Binding GoAccountsCommand}"
                            Header="{x:Static lang:Resources.AppMenu_AccountsText}" />
                        <NativeMenuItem
                            Command="{Binding GoStorageCommand}"
                            Header="{x:Static lang:Resources.AppMenu_StorageText}" />
                        <NativeMenuItemSeparator />
                        <NativeMenuItem
                            Command="{Binding ToggleFullScreenCommand}"
                            Gesture="F11"
                            Header="{x:Static lang:Resources.AppMenu_FullScreenText}" />
                    </NativeMenu>
                </NativeMenuItem.Menu>
            </NativeMenuItem>
            <NativeMenuItem Header="{x:Static lang:Resources.AppMenu_HelpText}">
                <NativeMenuItem.Menu>
                    <NativeMenu>
                        <NativeMenuItem
                            Command="{Binding AboutCommand}"
                            Header="{x:Static lang:Resources.AppMenu_AboutText}" />
                        <NativeMenuItemSeparator />
                        <NativeMenuItem
                            Command="{Binding CheckForUpdatesCommand}"
                            Header="{x:Static lang:Resources.AppMenu_CheckForUpdatesText}" />
                        <NativeMenuItem
                            Command="{Binding OpenGitHubCommand}"
                            Header="{x:Static lang:Resources.AppMenu_FeedbackText}" />
                    </NativeMenu>
                </NativeMenuItem.Menu>
            </NativeMenuItem>
        </NativeMenu>
    </NativeMenu.Menu>
    <Grid Name="Container">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="83*" />
            <ColumnDefinition
                Width="27*"
                MinWidth="200"
                MaxWidth="400" />
        </Grid.ColumnDefinitions>
        <Border
            x:Name="Main"
            Grid.Column="0">
            <DockPanel>
                <Panel>
                    <husk:Frame x:Name="Root" />
                </Panel>
            </DockPanel>
        </Border>
        <Border
            x:Name="Sidebar"
            Grid.Column="1"
            Background="{DynamicResource SidebarBackground}"
            BorderBrush="{DynamicResource SidebarBorder}"
            BorderThickness="0,0,1,0">
            <DockPanel>
                <Grid
                    x:Name="TitleBar"
                    DockPanel.Dock="Top"
                    IsVisible="{Binding $parent[app:MainWindow].IsTitleBarVisible, Mode=OneWay}"
                    ColumnDefinitions="*,Auto">
                    <Border
                        Grid.Column="0"
                        Grid.ColumnSpan="2"
                        Background="Transparent"
                        WindowDecorationProperties.ElementRole="TitleBar" />
                    <StackPanel Grid.Column="1">
                        <husk:ButtonGroup
                            IsVisible="{OnPlatform Default={x:True},
                                                   macOS={x:False}}"
                            BorderThickness="0">
                            <husk:ButtonGroup.Styles>
                                <Style Selector="Button">
                                    <Setter Property="Padding" Value="10" />
                                </Style>
                                <Style Selector="fi|SymbolIcon">
                                    <Setter Property="FontSize" Value="10" />
                                </Style>
                                <Style Selector="husk|IconLabel">
                                    <Setter Property="FontSize" Value="10" />
                                </Style>
                            </husk:ButtonGroup.Styles>
                            <Button
                                Click="MinimizeButton_OnClick"
                                Cursor="{x:Null}"
                                WindowDecorationProperties.ElementRole="MinimizeButton">
                                <fi:SymbolIcon Symbol="Subtract" />
                            </Button>
                            <Button
                                Click="ToggleMaximizeButton_OnClick"
                                Cursor="{x:Null}"
                                WindowDecorationProperties.ElementRole="MaximizeButton">
                                <husk:SwitchContainer
                                    TargetType="x:Boolean"
                                    Value="{Binding $parent[husk:AppWindow].IsMaximized, Mode=OneWay}">
                                    <husk:SwitchCase Value="True">
                                        <fi:SymbolIcon Symbol="ArrowMinimize" />
                                    </husk:SwitchCase>
                                    <husk:SwitchCase Value="False">
                                        <fi:SymbolIcon Symbol="Maximize" />
                                    </husk:SwitchCase>
                                </husk:SwitchContainer>
                            </Button>
                            <Button
                                Click="CloseButton_OnClick"
                                Cursor="{x:Null}"
                                WindowDecorationProperties.ElementRole="CloseButton">
                                <Button.Styles>
                                    <Style Selector="Button:pointerover fi|SymbolIcon">
                                        <Setter Property="Foreground" Value="{StaticResource ControlDangerForegroundBrush}" />
                                    </Style>
                                </Button.Styles>
                                <fi:SymbolIcon Symbol="Dismiss">
                                    <fi:SymbolIcon.Transitions>
                                        <Transitions>
                                            <BrushTransition
                                                Easing="SineEaseOut"
                                                Property="Foreground"
                                                Duration="{StaticResource ControlFasterAnimationDuration}" />
                                        </Transitions>
                                    </fi:SymbolIcon.Transitions>
                                </fi:SymbolIcon>
                            </Button>
                        </husk:ButtonGroup>
                    </StackPanel>
                    <StackPanel
                        Grid.Column="0"
                        VerticalAlignment="Center"
                        IsHitTestVisible="False"
                        Orientation="Horizontal"
                        Spacing="6">
                        <StackPanel.Margin>
                            <OnPlatform
                                Default="12,0,0,-9"
                                macOS="72, 8, 0, -1" />
                        </StackPanel.Margin>
                        <StackPanel.Effect>
                            <DropShadowEffect
                                BlurRadius="6"
                                Opacity="0.1" />
                        </StackPanel.Effect>
                        <Image
                            Width="{StaticResource MediumFontSize}"
                            Height="{StaticResource MediumFontSize}"
                            VerticalAlignment="Center"
                            RenderOptions.BitmapInterpolationMode="HighQuality"
                            Source="/Assets/Icon.png" />
                        <TextBlock
                            VerticalAlignment="Center"
                            FontSize="{StaticResource SmallFontSize}"
                            Text="{Binding $parent[Window].Title, Mode=OneWay}" />
                    </StackPanel>
                </Grid>
                <Grid
                    Margin="9"
                    ColumnDefinitions="48,*,48"
                    ColumnSpacing="8"
                    RowDefinitions="Auto,Auto,Auto,Auto,*,Auto,Auto"
                    RowSpacing="8">
                    <Button
                        Grid.Row="0"
                        Grid.Column="0"
                        Grid.ColumnSpan="2"
                        Command="{Binding NavigateCommand}"
                        CommandParameter="{x:Type app:LandingPage}"
                        Theme="{StaticResource OutlineButtonTheme}">
                        <StackPanel
                            Orientation="Horizontal"
                            Spacing="8">
                            <fi:SymbolIcon
                                VerticalAlignment="Center"
                                FontSize="18"
                                IconVariant="Color"
                                Symbol="Home" />
                            <TextBlock
                                VerticalAlignment="Center"
                                Text="{x:Static lang:Resources.MainWindow_HomeButtonText}" />
                        </StackPanel>
                    </Button>
                    <Button
                        Grid.Row="0"
                        Grid.Column="2"
                        Command="{Binding NavigateCommand}"
                        CommandParameter="{x:Type app:NewInstancePage}"
                        Theme="{StaticResource OutlineButtonTheme}">
                        <fi:SymbolIcon
                            FontSize="18"
                            IconVariant="Color"
                            Symbol="AddStarburst" />
                    </Button>
                    <Button
                        Grid.Row="1"
                        Grid.Column="0"
                        Grid.ColumnSpan="3"
                        Classes="Primary"
                        Command="{Binding NavigateCommand}"
                        CommandParameter="{x:Type app:MarketplacePortalPage}">
                        <StackPanel
                            Orientation="Horizontal"
                            Spacing="8">
                            <fi:SymbolIcon
                                VerticalAlignment="Center"
                                FontSize="18"
                                IconVariant="Color"
                                Symbol="Gift" />
                            <StackPanel
                                VerticalAlignment="Center"
                                Spacing="0">
                                <TextBlock Text="{x:Static lang:Resources.MainWindow_MarketplaceButtonText}" />
                                <TextBlock
                                    FontSize="{StaticResource SmallFontSize}"
                                    Opacity="0.7"
                                    Text="{x:Static lang:Resources.MainWindow_MarketplaceButtonSubText}" />
                            </StackPanel>
                        </StackPanel>
                    </Button>
                    <Button
                        Grid.Row="2"
                        Grid.Column="0"
                        Command="{Binding NavigateCommand}"
                        CommandParameter="{x:Type app:RecipesPage}"
                        Theme="{StaticResource OutlineButtonTheme}"
                        ToolTip.Tip="{x:Static lang:Resources.MainWindow_RecipesButtonText}">
                        <fi:SymbolIcon
                            FontSize="18"
                            IconVariant="Color"
                            Symbol="ListBar" />
                    </Button>
                    <Button
                        Grid.Row="2"
                        Grid.Column="1"
                        Command="{Binding NavigateCommand}"
                        CommandParameter="{x:Type app:InstancesPage}"
                        Theme="{StaticResource OutlineButtonTheme}">
                        <StackPanel
                            Orientation="Horizontal"
                            Spacing="8">
                            <fi:SymbolIcon
                                FontSize="18"
                                IconVariant="Color"
                                Symbol="Apps" />
                            <TextBlock
                                VerticalAlignment="Center"
                                Text="{x:Static lang:Resources.MainWindow_InstancesButtonText}" />
                        </StackPanel>
                    </Button>
                    <Button
                        Grid.Row="2"
                        Grid.Column="2"
                        Command="{Binding NavigateCommand}"
                        CommandParameter="{x:Type app:AccountsPage}"
                        Theme="{StaticResource OutlineButtonTheme}"
                        ToolTip.Tip="{x:Static lang:Resources.MainWindow_AccountButtonText}">
                        <fi:SymbolIcon
                            FontSize="18"
                            IconVariant="Color"
                            Symbol="People" />
                    </Button>
                    <husk:Divider
                        Grid.Row="3"
                        Grid.Column="0"
                        Grid.ColumnSpan="3" />
                    <husk:DropContainer
                        Grid.Row="4"
                        Grid.Column="0"
                        Grid.ColumnSpan="3"
                        DragOver="DropContainer_OnDragOver"
                        Drop="DropContainer_OnDrop">
                        <husk:DropContainer.Overlay>
                            <Panel>
                                <Rectangle
                                    Fill="{StaticResource ControlInteractiveBackgroundBrush}"
                                    RadiusX="6"
                                    RadiusY="6"
                                    Stroke="{StaticResource ControlInteractiveBorderBrush}"
                                    StrokeDashArray="6,2"
                                    StrokeJoin="Round"
                                    StrokeLineCap="Round"
                                    StrokeThickness="2" />
                                <StackPanel
                                    Margin="24,18"
                                    VerticalAlignment="Center"
                                    Opacity="{StaticResource Overlay1Opacity}"
                                    Spacing="12">
                                    <fi:SymbolIcon
                                        HorizontalAlignment="Center"
                                        FontSize="36"
                                        Foreground="{StaticResource ControlWhiteForegroundBrush}"
                                        Symbol="CubeAdd" />
                                </StackPanel>
                            </Panel>
                        </husk:DropContainer.Overlay>
                        <app:EmptyContainer
                            Icon="Apps"
                            IsEmpty="{Binding View.Count, Converter={x:Static husk:NumberConverters.IsZero}, FallbackValue=True}"
                            Text="{x:Static lang:Resources.MainWindow_EmptyInstanceListText}">
                            <ScrollViewer>
                                <ItemsControl ItemsSource="{Binding View}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate DataType="app:InstanceEntryModel">
                                            <app:InstanceEntryButton
                                                Command="{Binding $parent[app:MainWindow].((app:MainWindowContext)DataContext).ViewInstanceCommand, FallbackValue={x:Null}}"
                                                CommandParameter="{Binding Basic.Key}"
                                                State="{Binding State, Mode=OneWay}">
                                                <app:InstanceEntryButton.ContextFlyout>
                                                    <MenuFlyout>
                                                        <MenuItem
                                                            Command="{Binding $parent[app:MainWindow].((app:MainWindowContext)DataContext).PlayCommand, FallbackValue={x:Null}}"
                                                            CommandParameter="{Binding Basic.Key}"
                                                            Header="{x:Static lang:Resources.MainWindow_PlayMenuText}"
                                                            Icon="{fie:SymbolIcon Symbol=Rocket,
                                                                                  FontSize={StaticResource MediumFontSize}}" />
                                                        <MenuItem
                                                            Command="{Binding $parent[app:MainWindow].((app:MainWindowContext)DataContext).DeployCommand, FallbackValue={x:Null}}"
                                                            CommandParameter="{Binding Basic.Key}"
                                                            Header="{x:Static lang:Resources.MainWindow_DeployMenuText}"
                                                            Icon="{fie:SymbolIcon Symbol=SettingsCogMultiple,
                                                                                  FontSize={StaticResource MediumFontSize}}" />
                                                        <MenuItem Header="-" />
                                                        <MenuItem
                                                            Command="{Binding $parent[app:MainWindow].((app:MainWindowContext)DataContext).ExportInstanceCommand, FallbackValue={x:Null}}"
                                                            CommandParameter="{Binding Basic.Key}"
                                                            Header="{x:Static lang:Resources.MainWindow_ExportModpackMenuText}"
                                                            Icon="{fie:SymbolIcon Symbol=FolderZip,
                                                                                  FontSize={StaticResource MediumFontSize}}" />
                                                        <MenuItem
                                                            Command="{Binding $parent[app:MainWindow].((app:MainWindowContext)DataContext).OpenFolderCommand, FallbackValue={x:Null}}"
                                                            CommandParameter="{Binding Basic.Key}"
                                                            Header="{x:Static lang:Resources.MainWindow_OpenFolderMenuText}"
                                                            Icon="{fie:SymbolIcon Symbol=FolderOpen,
                                                                                  FontSize={StaticResource MediumFontSize}}" />
                                                        <MenuItem Header="-" />
                                                        <MenuItem
                                                            Command="{Binding $parent[app:MainWindow].((app:MainWindowContext)DataContext).GotoSetupCommand, FallbackValue={x:Null}}"
                                                            CommandParameter="{Binding Basic.Key}"
                                                            Header="{x:Static lang:Resources.MainWindow_SetupMenuText}"
                                                            Icon="{fie:SymbolIcon Symbol=BoxMultiple,
                                                                                  FontSize={StaticResource MediumFontSize}}" />
                                                        <MenuItem
                                                            Command="{Binding $parent[app:MainWindow].((app:MainWindowContext)DataContext).GotoPropertiesCommand, FallbackValue={x:Null}}"
                                                            CommandParameter="{Binding Basic.Key}"
                                                            Header="{x:Static lang:Resources.MainWindow_PropertiesMenuText}"
                                                            Icon="{fie:SymbolIcon Symbol=WrenchScrewdriver,
                                                                                  FontSize={StaticResource MediumFontSize}}" />
                                                        <MenuItem Header="-" />
                                                        <MenuItem
                                                            IsVisible="{Binding !IsPinned}"
                                                            Command="{Binding $parent[app:MainWindow].((app:MainWindowContext)DataContext).PinCommand, FallbackValue={x:Null}}"
                                                            CommandParameter="{Binding Basic.Key}"
                                                            Header="{x:Static lang:Resources.MainWindow_PinMenuText}"
                                                            Icon="{fie:SymbolIcon Symbol=Pin,
                                                                                  FontSize={StaticResource MediumFontSize}}" />
                                                        <MenuItem
                                                            IsVisible="{Binding IsPinned}"
                                                            Command="{Binding $parent[app:MainWindow].((app:MainWindowContext)DataContext).UnpinCommand, FallbackValue={x:Null}}"
                                                            CommandParameter="{Binding Basic.Key}"
                                                            Header="{x:Static lang:Resources.MainWindow_UnpinMenuText}"
                                                            Icon="{fie:SymbolIcon Symbol=PinOff,
                                                                                  FontSize={StaticResource MediumFontSize}}" />
                                                    </MenuFlyout>
                                                </app:InstanceEntryButton.ContextFlyout>
                                            </app:InstanceEntryButton>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Spacing="4" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                            </ScrollViewer>
                        </app:EmptyContainer>
                    </husk:DropContainer>
                    <husk:Divider
                        Grid.Row="5"
                        Grid.Column="0"
                        Grid.ColumnSpan="3" />
                    <Button
                        Grid.Row="6"
                        Grid.Column="1"
                        Grid.ColumnSpan="2"
                        Command="{Binding NavigateCommand}"
                        CommandParameter="{x:Type app:SettingsPage}"
                        Theme="{StaticResource OutlineButtonTheme}">
                        <StackPanel
                            Orientation="Horizontal"
                            Spacing="8">
                            <fi:SymbolIcon
                                FontSize="18"
                                IconVariant="Color"
                                Symbol="Settings" />
                            <TextBlock
                                VerticalAlignment="Center"
                                Text="{x:Static lang:Resources.MainWindow_SettingsButtonText}" />
                        </StackPanel>
                    </Button>
                    <Button
                        Grid.Row="6"
                        Grid.Column="0"
                        husk:BadgeService.Classes="Primary"
                        husk:BadgeService.Content="{Binding UnreadNotificationCount}"
                        husk:BadgeService.IsVisible="{Binding UnreadNotificationCount, Mode=OneWay, Converter={x:Static husk:ObjectConverters.NotMatch}, ConverterParameter=0}"
                        Command="{Binding OpenNotificationSidebarCommand}"
                        Theme="{StaticResource OutlineButtonTheme}"
                        ToolTip.Tip="{x:Static lang:Resources.NotificationSidebar_NotificationsLabelText}">

                        <fi:SymbolIcon
                            FontSize="18"
                            IconVariant="Color"
                            Symbol="Alert" />
                    </Button>
                </Grid>
            </DockPanel>
        </Border>
    </Grid>
</husk:AppWindow>
