<husk:Dialog
    x:Class="Polymerium.Avalonia.Dialogs.RecipeEditorDialog"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:app="https://github.com/d3ara1n/Polymerium"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:husk="https://github.com/d3ara1n/Huskui.Avalonia"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Width="360"
    IsPrimaryButtonVisible="True"
    Message="{Translate {x:Static app:LanguageManager+Keys.RecipeEditorDialog_Prompt}}"
    mc:Ignorable="d">
    <StackPanel Spacing="8">
        <TextBox
            PlaceholderText="{Translate {x:Static app:LanguageManager+Keys.RecipeEditorDialog_NameWatermark}}"
            Text="{Binding $parent[app:RecipeEditorDialog].RecipeName, Mode=TwoWay}" />
        <TextBox
            MinHeight="72"
            AcceptsReturn="True"
            PlaceholderText="{Translate {x:Static app:LanguageManager+Keys.RecipeEditorDialog_DescriptionWatermark}}"
            Text="{Binding $parent[app:RecipeEditorDialog].RecipeDescription, Mode=TwoWay}"
            TextWrapping="Wrap" />
    </StackPanel>
</husk:Dialog>
