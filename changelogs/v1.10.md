# Changelog v1.10

## [1.10.4] - 2026-06-27

### ✨ Highlights ✨

- Rework the settings navigation into an inline sidebar
- Block launching a second instance and instead bring the already running window to the foreground
- Make deleting, resetting, or unlinking an instance require per-action confirmation instead of a shared unlock
- Prevent crashes when triggering a destructive action twice or with stale data by making profile removal idempotent

### Fixed

- Attempt to fix instance icons and settings disappearing after an update restart by freezing the data root at first access
- Fix directory-membership checks matching sibling folders that share a name prefix
- Fix path and file name comparisons remaining case-sensitive on macOS
- Prevent crashes when triggering a destructive action twice or with stale data by making profile removal idempotent

### Added

- Block launching a second instance and instead bring the already running window to the foreground

### Changed

- Rework the settings navigation into an inline sidebar
- Update file picking to return the on-disk casing of matched files
- Make deleting, resetting, or unlinking an instance require per-action confirmation instead of a shared unlock
- Wrap the about dialog link row so buttons no longer overflow on narrow windows

### Removed

-

## [1.10.3] - 2026-06-26

### ✨ Highlights ✨

- Rework the instance navigation rail into a collapsible sidebar with text labels that remembers its expansion state
- Restructure the settings page with a floating section navigation that jumps to a section on click and highlights the active one while scrolling
- Restructure the maintenance action area into a grouped tools panel that separates safe actions from destructive ones with a labeled divider

### Fixed

- Fix duplicated labels in Dependency Graph page
- Fix the Linux AppImage crashing on startup (#72)

### Added

- Require retyping a verification code to confirm destructive data-clearing operations

### Changed

- Rework the instance navigation rail into a collapsible sidebar with text labels that remembers its expansion state
- Restructure the settings page with a floating section navigation that jumps to a section on click and highlights the active one while scrolling
- Restructure the maintenance action area into a grouped tools panel that separates safe actions from destructive ones with a labeled divider

### Removed

-

## [1.10.2] - 2026-06-24

### ✨ Highlights ✨

- 新增外置登录账号创建时主流验证服务器的快捷选择入口
- 新增依赖图中缺失依赖的标记与数量统计

### Fixed

- 修复无账户时实例启动按钮仍可点击的问题
- 修复依赖图仅展开已安装包的直接依赖而无法显示外部依赖链的问题

### Added

- 新增外置登录账号创建时主流验证服务器的快捷选择入口
- 新增依赖图中缺失依赖的标记与数量统计
- 新增依赖图选中节点的前置与附属依赖列表

### Changed

- 修改本地化文本以及为部分未本地化的文本进行本地化
- 重新布局快捷栏内容
- 统一侧边栏导航按钮的视觉风格
- 本地化实例活动记录卡片的操作标签与快照管理的提示文案
- 优化依赖图图标的加载速度

### Removed

-

## [1.10.1] - 2026-06-21

### ✨ Highlights ✨

- 调整 macOS 上品牌数据目录的命名以匹配应用标识便于卸载工具清扫
- 新增实例依赖图对触摸板缩放与平移手势的支持

### Fixed

- 修复自定义数据目录的覆盖文件在目标目录尚不存在时会被静默忽略的问题
- 修复 HTTP 缓存与崩溃报告缓存被错误写入配置目录而非缓存目录的问题
- 修复实例包的依赖图中视觉效果在亮暗模式下不统一的问题

### Added

- 新增实例依赖图对触摸板缩放与平移手势的支持

### Changed

- 调整 macOS 上品牌数据目录的命名以匹配应用标识便于卸载工具清扫
- 调整实例依赖图滚轮默认为平移并将缩放改为需按住 Ctrl 或 Command 键触发

### Removed

- 移除命令行工具的 --home 参数改为统一使用环境变量或标记文件指定数据目录
-

## [1.10.0] - 2026-06-20

### ✨ Highlights ✨

- 为实例设置页添加已安装包依赖关系的可视化图表
- 支持使用系统定义的分类目录并在存在遗留目录时优先回退以提供兼容性
- 为实例部署进度条添加已下载文件数与总文件数的显示

### Fixed

- 修复整合包安装完成后实例条目消失、重启后才恢复的问题 (#POLY-111)
- 修复对话框总是布满容器宽的问题 (Huskui.Avalonia)
- 修复软件垃圾收集中忽略了快照数据库的孤儿数据的问题

### Added

- 为实例设置页添加已安装包依赖关系的可视化图表 (#POLY-86)
- 支持使用系统定义的分类目录并在存在遗留目录时优先回退以提供兼容性 (#POLY-112)
- 为实例部署进度条添加已下载文件数与总文件数的显示

### Changed

- 优化依赖图节点为带缩略图与作者信息的卡片样式 (#POLY-86)
- 现在位于 macOS 上时会优先使用自定义标题栏样式尽管存在已知问题

### Removed

-